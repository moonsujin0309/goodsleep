# 굿슬립 — 수면 앱 (모바일 웹)

Calm(백색소음·자연음·비주얼)과 Renight(상태별 나레이션·수면부채)의 장점만 합친 수면 웹앱.
**게임이 아니다.** 이 작업공간의 다른 프로젝트와 무관한 별개 프로젝트다.

진실 소스: [2026-08-12-굿슬립-설계문서.md](2026-08-12-굿슬립-설계문서.md) · 현황: [STATUS.md](STATUS.md)

## 이 앱이 푸는 문제

두 앱을 쓰고 남은 불만이 하나로 모인다: **반복**.
Renight는 AI가 매일 새로 만든다는데도 "결국 똑같다"고 느껴졌다.
생성 여부가 아니라 **체감 다양성**이 문제다. 이 앱의 존재 이유는 그것 하나다.

기능을 추가할 때 이 질문을 먼저 통과시킬 것 — *이게 밤마다 다르게 느껴지는 데 기여하는가?*

## 건드리기 전에 알아야 할 결정들

이유 없이 뒤집지 말 것. 전부 근거가 있다.

| 결정 | 이유 |
|---|---|
| 오디오는 `<audio>` 엘리먼트로만 재생. Web Audio 그래프 금지 | 화면을 끄면 iOS가 AudioContext를 정지시킨다. 수면 앱은 화면을 켜둘 수 없다 |
| 배경음 루프는 `<audio loop>` 가 아니라 **엘리먼트 2개 등파워 교대**(`SeamlessLoop`) | 파형 이음매는 이미 크로스페이드돼 있는데도 브라우저가 되감을 때 몇 ms를 흘린다. 백색소음에서는 그 짧은 무음이 "뚝" 하고 들린다. `loop=true` 는 교대를 놓쳤을 때의 안전망으로만 남겨 둔다 |
| 넘김 방식이 기기마다 다르다 — 볼륨이 먹으면 **실시간 √ 크로스페이드**, 안 먹으면(아이폰) **파형에 구운 페이드**(`synth.fadeEdges`) | 아이폰은 재생 중 볼륨을 못 바꿔서 겹치면 소리가 두 배가 된다. 그래서 오래 교대를 아예 꺼 뒀는데, 그 결과 되감기 틈이 그대로 들렸다(2026-09-02 "루프될 때 끊긴다"). 소스 머리·꼬리를 미리 √ 로 깎아 두면 겹치기만 해도 합이 일정하다. **둘을 같이 걸면 가운데가 파인다** — `forHandoff` 가 `VOLUME_SETTABLE` 일 때 원본을 그대로 돌려주는 이유다 |
| 배경 영상도 **`<video>` 2개 선형 교대**(`SceneRenderer._videoTick`). `#scene-video` 는 감싸는 `<div>` 다 | 영상의 끝 프레임과 첫 프레임이 달라서 되감을 때 "번쩍"인다. 층 전체 밝기(`is-on`·`is-dim`)는 감싸는 div 가 갖고, 안쪽 video 의 `opacity` 는 교대 전용이다 — 한 엘리먼트에 두 가지를 얹으면 서로 덮어쓴다 |
| 씬 7개 중 **눈·오로라·달과 구름은 캔버스 전용**(사진·영상 없음) | 파일 0바이트라 루프 자체가 없다. 대신 프레임마다 그라데이션을 수십 개 만드는 그리기는 금지 — 밤새 도는 그림이다. 오로라는 커튼당 그라데이션 1개, 구름은 스프라이트를 미리 그려 두고 밀기만 한다 |
| `sw.js` 의 앱 파일 fetch 는 **`cache: 'reload'`** | 없으면 브라우저 HTTP 캐시가 낡은 파일을 돌려줘서 "네트워크 우선"이 이름뿐이 된다. 배포해도 화면이 안 바뀌는 정체가 이것이다 |
| 배경음은 **앱을 여는 첫 터치**부터 흐른다 (세션 시작이 아니라) | 홈이 적막하면 시작 버튼이 "소리 켜는 버튼"이 된다. 자동재생 정책 때문에 제스처는 필요하다 |
| **재생(나레이션) 화면에 호흡 게이지를 두지 않는다.** 호흡 훈련은 홈의 별도 "호흡" 화면(`s-breathe`)이 맡는다 | 2026-08-29 분리. 대본에서 숫자 세기를 뺀 뒤로 말과 게이지를 이어줄 고리가 없고, 화면을 보게 만드는 것 자체가 수면에 역행한다. 나레이션의 `breath` 층은 말로만 이끈다. 호흡 화면은 패턴 3종(4-7-8 · 상자 4-4-4-4 · 4-6)에 링과 **남은 초 내려세기**를 쓴다 — 올려 세기는 "1"이 0초에 떠서 실제보다 빠르게 느껴졌다 |
| 대본에 **숫자 세기(하나·둘·셋)를 넣지 않는다** | 2026-08-26에 전량 제거했다. 한두 음절짜리 토막은 모델이 음절당 0.277초로 늘여 읽고(문장은 0.149초) 음량도 최대 5배 작게 내서, 단어가 아니라 앓는 소리로 들렸다. 당기면 경박해지고 두면 신음이라 답이 없었다. 호흡비는 말로 지시한다. `app.js` 의 게이지 위상 맞추기(`하나`를 듣고 되감기)는 이제 안 걸린다 |
| 나레이션 mp3 는 **발화 RMS 0.11로 맞춰 굽는다** (`tts_voxcpm.NORM_RMS`) | 음량이 파일마다 16.5배까지 벌어져 "목소리가 중간에 바뀐다"로 들렸다. 굽는 단계에서 맞추므로 앱은 손댈 게 없다 |
| **나레이션 중에 배경음을 누르지 않는다** (`Mixer.DUCK = 1`) | 순서가 거꾸로였다. 배경음 크기는 사용자가 준비 화면에서 정하는 값인데 앱이 다시 누르면 그 설정이 무의미해진다. 맞춰야 하는 쪽은 목소리이고, 그건 `NORM_RMS` 로 이미 키웠다. 배경음을 최대로 올려도 실효 RMS 0.0162 대 목소리 0.11 이라 6.8배 위다. 목소리가 묻히면 `DUCK` 을 0.6 → 0.4 로 내린다 |
| 나레이션은 통짜가 아니라 **조각 3층 조립** | 통짜 10개는 10일이면 반복을 체감한다. 조각 21개면 조합 300개 |
| 중복 배제 폭은 풀의 **1/3** (`pool-1` 아님) | `pool-1`을 배제하면 후보가 하나만 남아 선택이 결정론적 순환이 된다. 테스트가 잡았다 |
| 기상 시각 = **알람을 끄는 순간** | 웹앱이 확실히 아는 유일한 신호 |
| 나레이션은 **남성 목소리**, 엔진은 **VoxCPM2 · 시드 42 고정** | Google Chirp3-HD 남성 16개를 다 들어도 명상에 맞는 톤이 없었다 ("늙은 교수 같다"). 시드를 바꾸면 속도가 아니라 **목소리 자체가 바뀐다** — 속도 조절에 쓰지 말 것. 확정값과 배포 주의사항은 STATUS.md '음성 엔진 교체' 절 |
| 목소리는 **`tools/anchor/voice.wav` 가 정한다.** 묘사(`DESIGN`)가 아니다 | 조각마다 `generate()` 를 새로 부르면 **시드를 고정해도 목소리가 달라진다.** 실측 MFCC 거리가 층 사이 67까지 벌어졌고, 사용자가 "다른 사람 같다"고 짚은 층이 정확히 그 둘이었다. 음량·속도로는 못 맞춘다. 이 파일을 바꾸면 **전량 재생성**이다. `tools/anchor/README.md` 참조 |
| 속도·음량을 **고르게 만들수록 어색해진다** | 억양은 토막 사이·조각 사이의 차이에서 나온다. 토막별 음량 맞추기(`NORM_RMS`)·전체 공통 속도 목표·좁은 밴드(1.15)를 넣어 봤다가 각각 "평평하다"·"어색하다"·"뻣뻣하다"로 되돌렸다. 지금은 **조각 통째로 한 배수**만 걸어 조각 *사이*만 맞추고(`PIECE_RMS`), 조각 *안*은 그대로 둔다 |
| 말을 느리게 하려면 **`SLOW` 가 아니라 앵커를 늘린다** | 뽑은 뒤 시간축으로 늘리면 1.45배가 천장이다(자음이 뭉개진다). 앵커를 늘려 두면 모델이 처음부터 그 속도로 말한다 — 실측 앵커 1.25배 → 결과 1.27배. 가공이 아니라 생성이라 천장이 없다. 그래서 `SLOW = 1.0`(끔)이고 앵커에 1.43배가 걸려 있다 |
| **느림은 말과 침묵 두 곳에서 온다** | 말 늦추기는 한 문장 *안*이 늘어지고, `sentenceGap` 은 문장 *사이*가 벌어진다. 침묵은 음질 손해가 0이라 먼저 쓴다. 현재 말 0.139초/글자 · 침묵 기준 5.0초(층마다 1.0~2.1배로 벌어져 5.0~10.5초) |
| 볼륨 기본값은 **배경음·목소리 둘 다 50** | 100이면 천장에 붙어 더 키울 수가 없다. 배경음 배율은 2026-09-02에 0.24 → **0.48**로 올렸다 — 폰에서 직접 맞춘 "배경음 100"이 슬라이더 50에서 나오게 한 것이라 들리는 크기는 그대로다. **배율을 바꾸면 저장된 값도 같이 옮겨야 한다**(`bedScale2`), 안 그러면 쓰던 사람에게만 두 배가 된다 |
| 자막 뒤에는 **어두운 판**을 깐다 (`.caption` 배경). **`backdrop-filter` 는 쓰지 않는다** | 씬 스크림만으로는 파도 마루·모닥불처럼 밝은 사진 위에서 흰 글씨가 묻힌다. 첫 3초는 문장이 없으므로 `:empty` 일 때는 판을 지운다. 블러를 걸었다가 되돌렸다 — **iOS Safari 는 `backdrop-filter` 를 요소의 opacity 와 무관하게 전체 강도로 그려서**, 딤이 되면 판·글씨만 흐려지고 블러만 남아 밝은 영상이 그대로 번졌다 (2026-09-02 "글씨가 아예 안 보여") |
| 딤에서 자막은 **요소가 아니라 글자 색만** 잦아든다 (`color` 0.38, `opacity` 아님) | 요소째 흐리면 어두운 판까지 투명해져 밝은 씬 위에서 대비가 사라진다. 판이 남으면 화면은 오히려 더 어두워지므로 "밤새 밝은 화면은 방해다"와도 맞는다 |
| 재생 화면 자막 높이는 **`svh` 로 가둔다** (`max-height: 46svh`). `max-height: 100%` 금지 | flex 로 정해진 높이를 백분율로 되받는 모양이라 Safari 에서 풀린다 — 자막이 아래 버튼을 화면 밖으로 밀어냈고 데스크톱에서는 재현되지 않았다. `.screen`·`.play-body` 의 `min-height: 0` 도 같이 있어야 한다 |
| 나레이션은 **문장 단위로 끊어** 읽히고 침묵은 앱이 넣는다 | TTS 모델은 긴 침묵을 오류로 보고 줄인다. 어떤 엔진을 쓰든 이 방식이라야 침묵이 정확히 들어간다 |
| 침묵 길이는 **문장 길이에 비례** | "걱정." 뒤에 온전한 2.4초를 넣으면 리듬이 끊긴다 |
| 낮잠은 밤 기록과 **분리 저장** | 하루 여러 번 가능하고, 밤에 앱을 안 쓴 날에도 기록해야 한다 |
| 낮잠도 **밤과 같은 준비 화면**을 쓴다 | 프리셋·배경·직접 입력을 통째로 재사용한다. 단위가 바뀌면 직접 입력값은 초기화한다 |
| 밤 기록은 **날짜당 하나** (`upsertNight`) | 둘이면 부채는 둘 다 더하고 차트는 하나만 그려 숫자가 어긋난다 |
| 부채 상한 20시간 | 무한히 쌓이면 숫자가 절망스러워지고 사용자는 앱을 지운다 |
| 사운드는 **전부 코드 합성**. 음원 파일 금지 | 무료 라이브러리는 "소스 파일과 함께 재배포"를 금지한다 — 공개 저장소라 못 올린다. 합성은 라이선스가 없고 루프 이음매도 없다. `.gitignore` 가 오디오 확장자를 막고 있다 |
| 사운드 정규화는 **RMS 기준** (피크 아님) | 귀는 평균 에너지로 크기를 느낀다. 피크로 맞추면 같은 슬라이더에서 모닥불과 백색소음이 7배 차이로 들린다 |
| 모노럴/바이노럴 비트 **넣지 않음** | Renight에서 듣기 싫었던 바로 그 부분 |
| 배경은 순백(`#FFFFFF`) 금지, 어둠은 따뜻하게 | 청색광은 멜라토닌을 억제한다. 취향이 아니라 기능이다 |
| 터치 타겟 56px | 44px 권장보다 크다. 반쯤 잠든 채 어둠 속에서 누른다 |

## 구조

```
index.html  style.css  manifest.json
app.js       화면 전환 · 저장 · 세션
audio.js     믹서 · 나레이션 재생 · 백그라운드 유지 · 알람
synth.js     사운드 합성 DSP (스테레오 44.1kHz)  ← 순수 함수. node 로 렌더 가능
narration.js 조립 엔진 · 중복 회피      ← 순수 함수
sleep.js     타이머 · 수면부채 계산      ← 순수 함수
scenes.js    캔버스 비주얼
data/        narration.json · sounds.json
audio/       음원 넣는 곳 (README 참조)
test.js      sleep.js + narration.js 검증
```

빌드 없음. 정적 파일 + ES 모듈. `file://` 로는 안 열린다 (모듈·fetch 때문에 서버 필요).

```bash
cd D:/클로드/굿슬립 && python -m http.server 8123
```

## 배포 — push = 실서비스

https://moonsujin0309.github.io/goodsleep/ (public 저장소, Pages 는 `gh-pages` 브랜치 기준)

```bash
git push origin main gh-pages
```

**두 브랜치를 같이 올려야 사이트가 갱신된다.** `main` 만 올리면 코드만 바뀌고 배포는 그대로다.

## 규칙

- **계산 로직은 `sleep.js` / `narration.js` 에만 둔다.** 이 둘은 브라우저 API를 쓰지 않는다 —
  `test.js` 가 node로 바로 불러야 하기 때문이다. 여기 손대면 테스트를 같이 고친다
- 콘텐츠(나레이션 조각·사운드 레이어)는 **JSON만 수정**한다. 코드에 상태별 분기를 만들지 말 것.
  시퀀스 구성도 매니페스트의 `sequence` 가 정한다
- 표시 이름은 `app.js` 의 `APP_NAME` 한 곳에만 있다. "굿슬립"은 서술적이라 상표로 소유하기 어렵고,
  나중에 바뀔 수 있다. 다른 곳에 하드코딩하지 말 것
- UI 문구는 짧은 평서문 존댓말. 시적인 문장을 쓰지 않는다 (이름이 담백하므로 말투도 담백하게)
- 변경 후 반드시 `node test.js`
