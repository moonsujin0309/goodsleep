# 굿슬립 — 수면 앱 (모바일 웹)

Calm(백색소음·자연음·비주얼)과 Renight(상태별 나레이션·수면부채)의 장점만 합친 수면 웹앱.
**게임이 아니다.** 이 작업공간의 다른 프로젝트와 무관한 별개 프로젝트다.

진실 소스: [2026-08-12-굿슬립-설계문서.md](2026-08-12-굿슬립-설계문서.md) · 현황: [STATUS.md](STATUS.md)

## 이 앱이 푸는 문제

두 앱을 쓰고 남은 불만이 하나로 모인다: **반복**.
Renight는 AI가 매일 새로 만든다는데도 "결국 똑같다"고 느껴졌다.
생성 여부가 아니라 **체감 다양성**이 문제다. 이 앱의 존재 이유는 그것 하나다.

기능을 추가할 때 이 질문을 먼저 통과시킬 것 — *이게 밤마다 다르게 느껴지는 데 기여하는가?*

## 건드리기 전에 알아야 할 결정들

이유 없이 뒤집지 말 것. 전부 근거가 있다.

| 결정 | 이유 |
|---|---|
| 오디오는 `<audio>` 엘리먼트로만 재생. Web Audio 그래프 금지 | 화면을 끄면 iOS가 AudioContext를 정지시킨다. 수면 앱은 화면을 켜둘 수 없다 |
| 나레이션은 통짜가 아니라 **조각 3층 조립** | 통짜 10개는 10일이면 반복을 체감한다. 조각 21개면 조합 300개 |
| 중복 배제 폭은 풀의 **1/3** (`pool-1` 아님) | `pool-1`을 배제하면 후보가 하나만 남아 선택이 결정론적 순환이 된다. 테스트가 잡았다 |
| 기상 시각 = **알람을 끄는 순간** | 웹앱이 확실히 아는 유일한 신호 |
| 낮잠은 밤 기록과 **분리 저장** | 하루 여러 번 가능하고, 밤에 앱을 안 쓴 날에도 기록해야 한다 |
| 밤 기록은 **날짜당 하나** (`upsertNight`) | 둘이면 부채는 둘 다 더하고 차트는 하나만 그려 숫자가 어긋난다 |
| 부채 상한 20시간 | 무한히 쌓이면 숫자가 절망스러워지고 사용자는 앱을 지운다 |
| 사운드는 **전부 코드 합성**. 음원 파일 금지 | 무료 라이브러리는 "소스 파일과 함께 재배포"를 금지한다 — 공개 저장소라 못 올린다. 합성은 라이선스가 없고 루프 이음매도 없다. `.gitignore` 가 오디오 확장자를 막고 있다 |
| 사운드 정규화는 **RMS 기준** (피크 아님) | 귀는 평균 에너지로 크기를 느낀다. 피크로 맞추면 같은 슬라이더에서 모닥불과 백색소음이 7배 차이로 들린다 |
| 모노럴/바이노럴 비트 **넣지 않음** | Renight에서 듣기 싫었던 바로 그 부분 |
| 배경은 순백(`#FFFFFF`) 금지, 어둠은 따뜻하게 | 청색광은 멜라토닌을 억제한다. 취향이 아니라 기능이다 |
| 터치 타겟 56px | 44px 권장보다 크다. 반쯤 잠든 채 어둠 속에서 누른다 |

## 구조

```
index.html  style.css  manifest.json
app.js       화면 전환 · 저장 · 세션
audio.js     믹서 · 나레이션 재생 · 백그라운드 유지 · 알람
narration.js 조립 엔진 · 중복 회피      ← 순수 함수
sleep.js     타이머 · 수면부채 계산      ← 순수 함수
scenes.js    캔버스 비주얼
data/        narration.json · sounds.json
audio/       음원 넣는 곳 (README 참조)
test.js      sleep.js + narration.js 검증
```

빌드 없음. 정적 파일 + ES 모듈. `file://` 로는 안 열린다 (모듈·fetch 때문에 서버 필요).

```bash
cd D:/클로드/굿슬립 && python -m http.server 8123
```

## 배포 — push = 실서비스

https://moonsujin0309.github.io/goodsleep/ (public 저장소, Pages 는 `gh-pages` 브랜치 기준)

```bash
git push origin main gh-pages
```

**두 브랜치를 같이 올려야 사이트가 갱신된다.** `main` 만 올리면 코드만 바뀌고 배포는 그대로다.

## 규칙

- **계산 로직은 `sleep.js` / `narration.js` 에만 둔다.** 이 둘은 브라우저 API를 쓰지 않는다 —
  `test.js` 가 node로 바로 불러야 하기 때문이다. 여기 손대면 테스트를 같이 고친다
- 콘텐츠(나레이션 조각·사운드 레이어)는 **JSON만 수정**한다. 코드에 상태별 분기를 만들지 말 것.
  시퀀스 구성도 매니페스트의 `sequence` 가 정한다
- 표시 이름은 `app.js` 의 `APP_NAME` 한 곳에만 있다. "굿슬립"은 서술적이라 상표로 소유하기 어렵고,
  나중에 바뀔 수 있다. 다른 곳에 하드코딩하지 말 것
- UI 문구는 짧은 평서문 존댓말. 시적인 문장을 쓰지 않는다 (이름이 담백하므로 말투도 담백하게)
- 변경 후 반드시 `node test.js`
