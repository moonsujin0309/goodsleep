# 굿슬립 — 현황

> 최종 갱신: 2026-08-12

## 한 줄

프로토타입 동작함. 전체 흐름(상태 선택 → 나레이션 → 알람 → 수면부채 리포트)이 폰에서 돌아간다.
**실제 오디오 파일이 아직 하나도 없다** — 나레이션은 브라우저 내장 TTS로, 노이즈는 코드 합성으로 임시 재생 중.

## 상태

| 항목 | 상태 |
|---|---|
| 화면 흐름 (홈·준비·재생·알람·기록·낮) | 됨 |
| 나레이션 조립 엔진 + 중복 회피 | 됨. 40회 재생 시 조합 34종, 연속 중복 0 |
| 나레이션 대본 | 7개 상태 × 조각 총 50개 작성 완료 (JSON) |
| 사운드 믹서 | 됨. 백색/핑크/브라운은 코드 합성으로 재생, 나머지 7종은 "음원 없음" 잠금 |
| 수면 타이머 · 알람 · 수면부채 | 됨. 낮잠 타이머 + 수동 입력 포함 |
| 캔버스 배경 4종 (별하늘·비·파도·모닥불) | 됨 |
| 검증 | `node test.js` 29개 통과 |
| 접근성 | 대비 AA 통과, 터치 44px 이상, 가로 오버플로 없음, reduced-motion 대응 |
| **실제 음원** | **없음** |
| **TTS 나레이션 음성** | **없음** (브라우저 내장 TTS가 임시로 읽는 중) |
| 실기기(폰) 테스트 | 안 함 |

## 다음 할 일

1. **폰에서 밤새 한 번 재워보기.** 이게 가장 급하다. 화면을 끈 채 소리가 유지되는지,
   알람이 실제로 우는지는 데스크톱에서 확인할 수 없다. 첫 밤은 폰 기본 알람도 같이 켜둘 것
2. **무료 음원 받아서 `audio/loops/` 채우기** (Freesound CC0 · Pixabay · YouTube 오디오 보관함).
   루프 이음매 크로스페이드 필수. 넣으면 코드 수정 없이 바로 붙는다 — `audio/README.md` 참조
3. **`racing` 상태 하나만 TTS로 뽑아서 품질 확정.** 문장별 렌더 → 무음 직접 삽입 → 노멀라이즈.
   마음에 들면 그 설정을 나머지 6개 상태에 복제. 전부 뽑고 나서 다시 하는 게 제일 비싸다
4. 실사용 며칠 후 조각 수 재검토 — 조합이 부족하면 `body` 층부터 늘린다

## 작업 로그

### 2026-08-12 — 기획 + 프로토타입 구현

- Calm / Renight 실사용 불만을 정리해 문제를 "반복"으로 특정. 설계문서 작성
- 리서치: 일반 TTS 모델은 긴 침묵을 오류로 보고 줄인다 → 실시간 생성 불가, 미리 생성이 맞음을 확인
- 이름 `굿슬립` 확정. 영문 GoodSleep은 동종 앱이 이미 여럿이고 서술적이라
  상표로 소유하기 어려움 → `APP_NAME` 상수 한 곳으로 분리해 교체 비용을 0으로 만듦
- 전체 구현 (11개 파일). 정적 파일 + ES 모듈, 빌드 없음
- **테스트가 잡은 버그**: 중복 배제 폭을 `pool-1`로 두니 후보가 하나만 남아
  선택이 결정론적 순환이 됨 — 조합 36개가 12개만 돌았다. 1/3로 줄여 해결
- **검증 중 발견**: 같은 날짜 밤 기록이 둘이면 부채는 둘 다 더하는데 차트는 하나만 그림.
  `upsertNight`로 날짜당 하나 보장
- 대비 검사에서 3차 텍스트 4.47 (AA 미달) → 토큰 조정해 5.20
