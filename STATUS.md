# 굿슬립 — 현황

> 최종 갱신: 2026-08-25

## 한 줄

**배포됨 → https://moonsujin0309.github.io/goodsleep/** (2026-08-19, acf675e)
전체 흐름(상태 선택 → 나레이션 → 알람 → 수면부채 리포트)이 폰에서 돌아간다.
**나레이션 7개 상태 전부 녹음 음성**(VoxCPM2 로컬 생성) · 사운드 10종 합성 v2(스테레오)
· 알람 차임 · 호흡 게이지 · 홈 히어로 · 오프라인 SW 까지 실서비스에 반영됨.

> 2026-08-24 작업분(볼륨·시작 순서·이음매 없는 루프·일시정지·소리 3종·씬 3종)은 **로컬에만 있다.**
> 사운드는 13종, 씬은 7종이 됐다. 아래 "다음 할 일" 0번 참조.

## 배포

git 저장소다. **`main` 에 push 하면 실서비스에 반영된다.**

```
origin  https://github.com/moonsujin0309/goodsleep.git  (public)
Pages   gh-pages 브랜치 기준 자동 빌드 (푸시로 자동 활성화됨)
```

`main` 을 고쳤으면 `git push origin main gh-pages` 로 둘 다 올려야 사이트가 갱신된다.

## 상태

| 항목 | 상태 |
|---|---|
| 화면 흐름 (홈·준비·재생·알람·기록·낮) | 됨 |
| 나레이션 조립 엔진 + 중복 회피 | 됨. 40회 재생 시 조합 34종, 연속 중복 0 |
| 나레이션 대본 | **7개 상태 전부 5층 구조**(정착·호흡·이완·심상·소실) 재작성 완료. 조각 81개, 조합 1,017개 |
| 사운드 믹서 | 됨. **13종 전부 코드 합성 v2** — 스테레오 44.1kHz, 공명체 기반 물방울·장작·처프, 상태별 레이어드 모델 (`synth.js`). 음원 파일 0개. 2026-08-24 에 먼 천둥·기내 소음·동굴 물방울 추가 |
| 수면 타이머 · 알람 · 수면부채 | 됨. 낮잠 타이머 + 수동 입력 포함 |
| 캔버스 배경 **7종** (별하늘·비·파도·모닥불·눈·오로라·달과 구름) | 됨. 뒤의 3종은 사진·영상 없이 캔버스만 쓴다 |
| 사운드 음질 | 클리핑 없음(피크 전부 ≤0.58 한도 내), RMS 편차 1.06배, 루프 이음매 없음, 생성 지연 최대 280ms |
| 검증 | `node test.js` 29개 통과 |
| 접근성 | 대비 AA 통과, 터치 44px 이상, 가로 오버플로 없음, reduced-motion 대응 |
| 배포 | GitHub Pages 라이브, HTTPS, PWA 매니페스트 동작 |
| 검색 노출 · 방문자 측정 | 태그·파일은 넣음. **측정 ID 미발급 · 서치콘솔 미등록 → 아직 숫자 안 쌓임** |
| **TTS 나레이션 음성** | **7개 상태 전부 완료. 2026-08-25 VoxCPM2 로 전면 교체.** voice design 남성 · 시드 42 · EQ 없음 · 토막 788개 11.0MB. **2026-08-25 (7) 에 자르기 버그를 고쳐 7개 상태 전량 재생성(배포됨).** Google Chirp3-HD 는 폐기 (아래 '음성 엔진 교체' 절). 브라우저 TTS는 예비로 강등 |
| 오프라인(PWA) | 서비스 워커 추가. 앱 파일 네트워크 우선 + 캐시 폴백, mp3 캐시 우선(들은 것 저장) |
| 실기기(폰) 테스트 | 안 함 |
| 나레이션 서체 | 됨. 재생 화면 문장만 한글 명조(Gowun Batang). 나머지는 Pretendard 그대로 |

## 다음 할 일

0. **2·3단계 판(밤 15~17분, 7층 + 인지 섞기 + PMR)을 밤에 실제로 들어보기.** 볼 것 —
   ①이완 하체→상체 이음새 ②여운(tail) 침묵(~19초)이 과한지 (과하면 배수 4.6→4.0)
   ③새 조각 목소리가 같은 사람으로 들리는지 ④racing 에서 단어 조각(ds)이 나오면
   단어 사이 간격(~7초)이 편한지 ⑤wired 에서 PMR 조각(pl·pu)이 나오면 긴장 유지
   (~7초)가 잠자리에서 무리가 없는지. 전부 침묵·배수 문제면 재생성 없이 숫자만 바꾼다.
1. 특정 기법이 싫으면 **JSON에서 그 조각만 빼면 된다** (풀 추가 방식이라 코드 무관).
1. **2026-09-02 판을 폰에서 확인** — 자막 판·레이아웃, 배경음 50이 예전 100과 같은지,
   그리고 **루프 이음매가 정말 안 들리는지**. 파도 44초·바람 36초마다 넘김이 오므로
   1분 이상 들어야 지나간다. 아이폰 교대는 데스크톱에서 재현이 안 되는 경로다
   (실기기가 유일한 판정 수단 — 수치로는 겹침 파워 오차 0 까지만 확인됐다).
1. **호흡 링·초읽기가 폰 화면에서 크기가 맞는지** (2026-09-01 판, 아직 못 봤다).
1. **앵커 감속·침묵 5.0초·볼륨 50/50 판을 밤에 실제로 들어보기.** 배포됨 (2026-08-26 v12).
   - **속도를 더 만지려면 `SLOW` 가 아니라 앵커를 늘린다** (`tools/anchor/README.md`).
     침묵만 늘리는 쪽은 설정에서 바로 되고 재생성이 필요 없다
   - 볼륨은 이제 배경음·목소리 둘 다 50에서 시작한다. 재생 화면에서 바로 조절된다

1. **(이전) 앵커로 목소리를 고정한 판.** 배포됨 (2026-08-26 v11).
   - **목소리를 바꾸려면 `tools/anchor/voice.wav` 를 바꾸고 전량 재생성한다.**
     `DESIGN` 묘사는 앵커가 있으면 안 쓰인다 (`tools/anchor/README.md`)
   - 속도·음량을 더 고르게 만들고 싶어도 **조각 안까지 건드리지 말 것** —
     두 번 시도했고 두 번 다 "평평하다"·"어색하다"로 되돌렸다 (작업 로그 (4) 참조)
   - 새 인트로(`settle` 21조각)가 이번에 처음 나갔다. 길이·순서가 맞는지 들어볼 것

1. **(이전) 숫자 빼고 12% 늦춘 나레이션 확인.** 배포됨 (2026-08-26 v8).
   - 되돌리려면 `audio/_backup-racing-old/`·`audio/_backup-old-all/` 의 옛 파일을 덮는다
     (다만 그건 감속·숫자제거 **이전** 판이다)
   - 속도를 더 만지려면 `tts_voxcpm.py` 의 `SLOW` 를 바꾸고 **모델부터 다시 뽑는다.**
     `reslow.py` 는 두 번 못 돌린다 (두 번 늘어나는 걸 막는 안전장치가 있다)
   - `breath/b3` 계열에 남은 0.6~0.96초 **내부** 침묵이 거슬리면,
     굽는 단계에서 긴 침묵을 줄이는 수를 쓴다 (자르기로는 못 고친다)
   - 숫자를 뺐으므로 `app.js` 의 호흡 게이지 위상 맞추기(`하나` 를 듣고 되감기)는
     이제 안 걸린다. 게이지는 자기 주기로 돈다 — 실제로 보고 어색하면 그때 손본다

1. **(이전 항목) 3차 재생성 나레이션 관련 남은 확인.** 배포는 됐다 (2026-08-25 v4).
   재귀 분할 + 속도 보정이 들어간 판이다. 조각 끝에서 갑자기 빨라지는 게 줄었는지가 관건.
   배경음도 같이 봤을 것 — 목소리가 이번에도 묻히면 `audio.js` `_target()` 의 덕킹 0.08 을 더 내린다
   (다만 0.08 이면 배경이 사실상 안 들리는 수준이라, 그때는 나레이션 mp3 자체를 키우는 게 맞다).
   - 속도 편차는 4차에서 잡았다 (중앙값 1.54배·최대 1.94배·거부선 초과 0개).
     그래도 갑자기 빨라지는 데가 있으면 `PACE_BAND` 를 1.25 보다 좁힌다
   - 여백 4.2초가 맞는지도 같이 본다. 숫자 하나라 설정에서 바로 조절된다
1. **2026-08-24 변경 9건을 직접 보고 듣기.** (배포됨)
   - 배경음이 이번엔 너무 작으면 `audio.js` `_target()` 의 0.35(마스터)·0.22(덕킹)를 조절한다
   - 루프 이음매는 파도 44초·바람 36초·천둥 60초마다 온다 — **1분 이상 들어야 넘김 구간이 지나간다**
   - 영상 번쩍임은 파도 16초·비·모닥불마다 온다 — 20초 이상 볼 것
   - 일시정지는 폰에서 잠금화면 버튼으로도 눌러 볼 것 (`setMediaSession` 연결)
   - **새 소리 3종은 검수용 WAV 를 `audio/_samples/` 에 뽑아 뒀다** (`node tools/render_beds.mjs`).
     특히 먼 천둥은 60초에 우르릉이 서너 번뿐이라 끝까지 들어야 판단이 선다
   - 새 씬 3종(눈·오로라·달과 구름)은 사진이 없으니 그라데이션 위에서만 판단하면 된다
1. **새 상태 6개 미리듣기 확인.** `audio/_samples/미리듣기-*.mp3` 6개를 들어보고 판단.
   (2026-08-21 확인: 나레이션 710개·서비스 워커·설정 UI 는 **이미 배포돼 있다.**
   "로컬에만 있다"고 적혀 있던 것은 오래된 기록이었다 — `origin/main` 과 `main` 이 같다)
2. **폰에서 밤새 한 번 재워보기.** 화면을 끈 채 소리가 유지되는지,
   알람이 실제로 우는지는 데스크톱에서 확인할 수 없다. 첫 밤은 폰 기본 알람도 같이 켜둘 것
3. 합성 사운드를 실제로 들어보고 파라미터 조정. 특히 풀벌레는 합성 티가 날 가능성이 높다
4. 실사용 며칠 후 조각 수 재검토 — 조합이 부족하면 층당 조각을 4~5개로 늘린다 (`rewrite_states.py`)
5. **GA4 측정 ID 발급 후 `index.html` 의 `G-XXXXXXXXXX` 교체 + 서치콘솔 등록.**
   바꾸기 전까지 GA는 스스로 꺼져 있어 아무 요청도 보내지 않는다.
   서치콘솔은 인증 `<meta>` 를 `index.html` 의 안내 주석 아래에 붙이고,
   사이트맵으로 `https://moonsujin0309.github.io/goodsleep/sitemap.xml` 을 제출한다.
   **둘 다 배포(push)된 뒤라야 인증·수집이 된다**

## TTS 조사 결과 (2026-08-12)

> ⚠️ **이 절은 2026-08-12 시점 기록이다.** 2026-08-25 에 VoxCPM2 로 전면 교체했다.
> 현행 내용은 바로 아래 '음성 엔진 교체' 절이다. 여기 적힌 Google/Edge 관련 결론은
> 그때의 판단 근거로만 읽을 것.

**지금 들리는 로봇 목소리의 정체는 `Microsoft Heami`다.** Windows 기본 한국어 음성이고 2013년대 것이다.
폰에는 훨씬 나은 게 기본 탑재되어 있다 (안드로이드 Google 한국어, iOS Yuna). 폰에서 먼저 들어볼 것.

비교 대상으로 쓰던 https://voice-gamja.netlify.app 은 **Edge TTS 무료 엔드포인트**(`ko-KR-InJoonNeural`)다.
사용자가 매긴 70점의 기준선이 이것이다.

**Edge TTS 직접 호출은 막혔다.** 2024년부터 `Sec-MS-GEC` 서명 토큰이 필요해졌고,
토큰을 계산해 붙여도 **HTTP 403** 이 돌아온다. 안티어뷰즈가 더 조여진 것으로 보인다.
설령 뚫려도 Microsoft 약관상 공개 배포 앱에 쓸 근거가 없다 — 음원 라이선스 때와 같은 기준으로 포기했다.

`pip install` 은 이 PC에서 **SSL 인증서 검증 실패**로 막힌다 (TLS 가로채는 보안 프로그램).
`--trusted-host` 로 우회할 수 있지만 머신의 패키지 검증을 끄는 것이라 하지 않았다.
`npm` 은 정상 동작한다.

### 유료 없이 품질을 올린 방법 (구현 완료)

엔진을 바꾸지 않고도 되는 것들을 먼저 했다.

- **문장 단위 분리 재생.** TTS 모델은 대화용이라 긴 침묵을 오류로 보고 줄인다.
  문장별로 끊어 읽히고 침묵은 앱이 직접 넣는다. 어떤 엔진을 쓰든 이 방식이면 침묵이 정확히 들어간다.
  웹 TTS 페이지가 절대 줄 수 없는 부분이다
- **침묵 길이를 문장 길이에 비례**시켰다. "걱정. 계획. 후회." 같은 한 단어 열거 뒤 2.4초는 리듬을 끊는다 → 0.84초
- **음성·속도·톤·침묵을 설정에서 조절** 가능하게 했다. 기기마다 음성이 달라서 사용자가 직접 맞춰야 한다
- 한국어 음성이 없는 기기에는 경고를 띄운다 (영어 음성이 한글을 읽으면 소리가 뭉개진다)

### 다음 단계 (엔진 교체가 필요할 때)

`narration.json` 의 조각에 `files: [...]` (문장별 mp3 경로)를 넣으면 재생기가 자동으로 파일을 쓴다.
코드 수정이 필요 없다. 생성 스크립트만 붙이면 된다.

무료로 갈 수 있는 정식 경로는 **Azure Speech 무료 티어(F0)** — 월 50만 자, `ko-KR-HyunsuMultilingualNeural`
같은 2024년대 음성을 정식 약관으로 쓸 수 있다. 계정 생성이 필요하다.

## 음원을 다운로드하지 않은 이유 (재검토 시 참고)

Mixkit·Pixabay 등 무료 라이브러리는 **"소스 파일과 함께 재배포"를 금지**한다.
공개 저장소에 mp3 를 올리는 것이 정확히 여기에 걸린다.
위키미디어 CC0 는 써도 되지만 20~90MB 원본 녹음이라 모바일에서 열리지 않고, 모닥불은 아예 없었다.

그래서 합성으로 전환했고, 결과적으로 이득이 더 컸다 —
라이선스 없음 / 0바이트 / **루프 이음매 없음**(주기 성분을 루프 길이의 정수 배로 맞춤).
`.gitignore` 가 오디오 확장자를 막고 있다. 나중에 진짜 녹음을 넣더라도
실수로 공개 저장소에 올라가 라이선스 위반이 되는 것을 막기 위해서다. CC0 로 확인한 것만 예외에 추가할 것.

## 음성 엔진 교체 — Google → VoxCPM2 (2026-08-25)

**왜 바꿨나.** Google Chirp3-HD Algieba 가 "늙은 남자 교수 같고 너무 빠르다"는 판정을 받았다.
EQ·속도를 아무리 만져도 그 인상이 안 빠져서 포기 상태였다.
한국어 Chirp3-HD 남성 음성 16개를 전부 뽑아 비교했지만 명상에 맞는 톤이 없었다 —
**고정된 목록에서 고르는 방식의 한계**였다. 참고로 Algieba 는 16개 중 15번째로 빠른 음성이라,
"너무 빠르다"는 것도 `rate` 탓만이 아니라 음성 선택 탓이었다.

VoxCPM2 는 목소리를 **말로 묘사해서 만든다**(voice design). 후보 개수에 안 갇힌다.
Apache-2.0 · 로컬 GPU(RTX 4070 Ti, 8GB면 충분) · API 비용 없음 · 48kHz.

**확정값** (바꾸려면 이유가 있어야 한다)

| 항목 | 값 | 근거 |
|---|---|---|
| 목소리 | `tools/tts_voxcpm.py` 의 `DESIGN` | 속삭임 강도 3단계 비교에서 중간(`quiet and soft-spoken`) 채택. 더 세면 ASMR·귓속말, 더 빼면 힘이 없다 |
| 시드 | 42 고정 | **시드는 속도 조절 수단이 아니다.** 같은 묘사로 7.8/13.6/17.8초가 나왔는데 목소리 자체가 달라져 전부 거부됐다 |
| EQ | 걸지 않음 | 저역 부스트(+3dB@120)가 "늙은 교수" 소리의 원인이었다 |
| 문장 안 쉼표 | 넣지 않음 | 길이는 늘지만(+25%) 귓속말이 심해진다 |
| 문장 사이 여백 | 3.6 → **4.2초** | 느림은 말이 아니라 침묵에서 가져온다. `app.js` 기본값 |

**통짜로 뽑아 자른다.** 문장별로 따로 생성하면 문장마다 속도가 제각각이라
(실측 최대 2.06배) 조각 끝에서 갑자기 빨라지는 게 들린다. 한 번의 생성 안에서는 저절로 균일하다.
자를 때는 무음 간격 크기가 아니라 **글자 수 대비 길이가 가장 잘 맞는 묶음**을 고른다 —
간격 순으로 자르면 모델이 안 쉰 자리에서 경계가 밀려 81조각 중 23개가 어긋났다.
그래도 모델이 아예 붙여 읽으면 나눌 지점이 없어서, 그때는 **문장별 생성으로 물러난다**
(81개 중 33개). 정렬은 정확해지고 대신 그 조각 안에서 속도가 덜 고르다.

**속도 균일화 (2026-08-25 4차로 마무리).** 숫자 세기 토막을 뺀 조각 내 편차가
한때 통짜 1.73배(최대 2.66)·문장별 1.96배(최대 3.75)였고 **81개 중 29개가 거부선을 넘었다.**
승인된 미리듣기가 1.53배, 거부된 것이 2.06배다.
재귀 분할 + 속도 보정으로 79개가 내려왔고, 남은 2개(`racing/release/r1`·`wired/release/r1`)는
**분할 단계를 올려가며 가장 고른 판을 고르게** 해서 잡았다 (`MAX_SPLIT 3`).
그 둘은 16문장짜리 몸 훑기라 한 덩어리 안에서 속도가 앞뒤로 흘렀다 —
0.229초/글자로 시작해 0.106 으로 끝나는 식이라, 보정 폭(33%) 안에서는 못 따라잡는다.
2단계로 쪼개니 2.17 → 1.63배, 3단계로 1.94배가 됐다.

**현재 실측** (mp3 에서 직접 잼): 편차 중앙값 **1.54배** · 최대 **1.94배** ·
거부선 초과 **0개** · 파일 788개 전부 존재 · 문장수 불일치 0.

**배포 시 필수.** mp3 는 서비스 워커가 **캐시 우선**으로 준다.
음성이 바뀌는 배포에서는 `sw.js` 의 `VER` 을 반드시 올린다. 안 올리면 옛 목소리가 그대로 나온다.
또한 `app.js` 는 저장된 설정이 기본값을 덮으므로, 기본값만 바꾸면 쓰던 사람에게는 반영되지 않는다.

## 작업 로그

### 2026-09-03 — 시나리오 3단계: 인지 섞기·PMR + 호흡 링 단장 (배포됨)

**상태별 기법 분화 — 교체가 아니라 풀 추가.** 기존 조각도 근거가 있어 밤마다 섞여 나온다.

- **racing 심상 풀 + 인지 섞기 3조각**(ds1~3, `shuffle: 2`). 도입 2문장 + 무관한
  2~4음절 명사 12개 — 앱(`audio.js _speak`)이 도입 뒤부터 순서를 매번 무작위로 튼다.
  단어 간격은 기존 침묵 공식이 저절로 ~7초(연구 프로토콜 8초)를 만든다.
  브라우저 3회 실행으로 도입 고정·매번 다른 순서 확인, 실재생도 확인.
  단어 mp3 실측 0.53~0.77초 — "앓는 소리" 문제 없음. racing 조합 **4,374개**
- **wired 이완 풀 + PMR 6조각**(pl1~3 하체 · pu1~3 상체). "…버텼다가, …놓습니다"
  문장 꼴이 쉼표 분할(>22자)과 맞물려 긴장 유지 ~7초 · 이완 감상 ~15초가 된다.
  힘은 "아프지 않을 만큼만"을 대본에 명시. wired 조합 **8,748개**
- 생성 9조각(114파일, 1.7MB)만 — 기존 재생성 0. 편차 상한 초과 없음(최대 1.58).
  매니페스트 파일 1,126개 전부 존재. 세션 길이 추정 racing 15:16~16:57 ·
  wired 14:01~16:16 (풀 섞임 반영), `node test.js` 31개 통과
- **호흡 링 단장**: 두께 2 → 4, 진행 호를 그라데이션으로 — 보라(라벤더→밤하늘)·
  멈추기는 등불색, 은은한 발광(drop-shadow). SVG defs 는 index.html
- **작은 폰 보완**: 세로 720px 미만에서 홈 히어로·호흡 링을 한 단계 축소
  (크게보다 스크롤 없이 다 보이는 쪽이 낫다)
- 대본 소스는 계속 `rewrite_states.py` (wired 는 STATES 안, racing 은
  `RACING_DRIFT_ADD` 멱등 합류). 어휘 소유 검사 통과

### 2026-09-02 (3) — 시나리오 2단계: 밤 7층·15~17분 + 아이폰 레이아웃 (배포됨)

**밤 4개 상태가 7층이 됐다**: 정착 → 호흡 → **이완 하체(release)** → **이완 상체(release2)**
→ 심상 → 소실 → **여운(tail)**. 조합 243 → **2,187개**.

- **새 조각 36개**(상태당 하체 3·상체 3·여운 3)를 기존 코퍼스 문체·상태별 동사군에 맞춰
  썼다. 대본 소스는 `rewrite_states.py` — racing 은 확정 층을 안 건드리고 `RACING_DEPTH`
  로 세 층만 패치한다. 어휘 소유(OWNED)는 복수 소유 집합이 됐다(무거워지·어깨 →
  release·release2). tail 은 소유 어휘를 아예 못 쓴다.
- **생성은 새 조각만** — tts_voxcpm 이 기존 파일을 건너뛰므로 재생성 0. 52분, mp3 389개
  5.4MB. 품질: **편차 2.06배 초과 없음**(최대 1.47), 쪼개 뽑은 조각 5개(2덩어리, 정상 범위).
- `LAYER_GAP` 에 release2 3.6 · tail 4.6 추가. 여운은 문장 사이 ~19초로 말이 뜸해지다
  사라진다 — "끝이 절벽" 진단의 처방.
- 옛 이완 조각 12개분 고아 mp3 167개 삭제 (매니페스트 대조 스윕, 그 외 고아 0).
- **실측(ffprobe)**: racing 16:17 · anxious 16:01 · wired 15:22 · unknown 15:31
  (범위 14:46~16:51) · awoken 1:24 유지 · 말:침묵 15:85.
  브라우저에서 7층 전부 시퀀스에 뽑히고 새 mp3 재생되는 것 확인, 콘솔 에러 0.
- **아이폰 레이아웃(375px 실측)**: 준비 화면 "일어날 시각" 라벨 세로 꺾임 수정(nowrap),
  홈 하단 버튼 3개("호흡·낮에 잠깐·기록")를 3등분 대신 내용 크기로 — "낮에 잠깐" 두 줄
  꺾임 해소. 나머지 화면은 문제 없음 (숨긴 브라우저 패널의 반투명 화면은 렌더 잔상이었다
  — DOM 불투명도 1 확인).
- 분석 문서의 호흡(2:30)·심상(4:00) 확장은 **안 했다** — 층 분할 + 여운만으로 목표에
  닿아서다. 3단계(기법 분화)만 남았다.

### 2026-09-02 (2) — 시나리오 1단계: 밤 세션 7분 → 10~12분 (배포됨)

분석 문서(`2026-09-02-나레이션-시나리오-분석.md`)의 1단계 세 가지. **mp3 재생성 없음.**

- **`LAYER_GAP` 상향** — settle 1.2 · breath 1.8 · release 3.6 · drift 3.0 · fade 3.2.
  이완 층 부위당 12초 → 약 30초. 시뮬레이션(ffprobe 실측 기반):
  racing 7:30→**11:37** · anxious 6:54→10:27 · wired 6:41→10:30 · unknown 6:27→9:52
- **상태별 `gapScale`** — 매니페스트에서 침묵 배수를 덮는다. `awoken: 0.7` 로
  90초 상한 유지(1:24). 코드는 `NarrationPlayer` 생성 옵션 하나.
- **자막을 지금 말하는 문장 하나만** — `onChunk` 사용. 조각 통째(평균 168자) 표시는
  읽는 행위 자체가 각성이라 수면에 역행했고, 잘림·스크롤 문제의 뿌리이기도 했다.
  조각 사이 침묵에는 판도 비운다(`:empty`).
- 브라우저 실측: 자막이 6.1s→13.4s→21.9s 에 한 문장씩 교체 · awoken 세션에서
  `gapScale 0.7` 확인 · 콘솔 에러 0 · `node test.js` 31개 통과.
- CLAUDE.md 결정 표 3행 추가/갱신(침묵 배수 · gapScale · 자막 한 문장).

### 2026-09-02 — 아이폰 루프 틈 제거 · 자막 판 · 배경음 두 배 (배포됨)

**폰에서 보고 나온 지적 넷을 잡았다.**

**1. 루프될 때 잠시 끊긴다 — 아이폰에서 교대가 아예 꺼져 있었다.**
`_tick()` 첫 줄이 `if (!VOLUME_SETTABLE) return` 이었다. 볼륨을 못 쓰는 기기에서
두 엘리먼트를 겹치면 둘 다 최대치로 나와 1.2초 동안 소리가 두 배가 되기 때문이었고,
그래서 `el.loop` 안전망에 맡겼는데 **그 되감기 틈이 그대로 들렸다** (파도 44초마다).

고친 방식은 **페이드를 파형에 굽는 것**(`synth.fadeEdges`). 소스 머리와 꼬리를 √ 램프로
미리 깎아 두면 두 벌을 겹쳐 트는 것만으로 합이 일정해진다 — 재생 중 볼륨을 한 번도
만질 필요가 없다. 볼륨이 먹는 기기는 기존 실시간 크로스페이드 그대로다
(둘 다 걸면 가운데가 파인다). `_xt` 는 그 굽힌 경로의 겹침 타이머다.

| 검증 | 결과 |
|---|---|
| `fadeEdges` 겹침 파워 (합성 파형, test.js 2개) | 합 = 1 정확 |
| 실제 파도 44초 파형, 겹침 544곳 | **파워 오차 0** · 가운데 원본 그대로 |
| 데스크톱 교대 강제 (끝 1.4초 전으로 밀기) | i 0→1, 넘기는 내내 합산 0.0864 고정 |

**아이폰 교대는 데스크톱에서 재현이 안 된다** — 실기기 확인이 남아 있다.

**2. 배경음 배율 0.24 → 0.48.** 폰에서 직접 맞춘 "배경음 100 · 목소리 50" 을
슬라이더 50/50 자리로 옮겼다. 100 에 붙어 있으면 더 키울 수가 없다.
저장된 값은 한 번만 반으로 옮긴다(`bedScale2`) — 새로 설치한 사람은 이미 새 기본값이라
건드리지 않으므로, 스프레드 전에 `savedSettings` 를 따로 들고 "저장된 값이 있었는가"를 본다.
실측: 새 설치 슬라이더 50 → waves **0.0864** (= 옛 슬라이더 100 과 같은 값) ·
옛 0.5 저장분 → 0.25 로 옮겨져 **0.0432** 그대로.

**3. 자막이 안 보인다 — 문장 뒤에 어두운 판을 깔았다.** 씬 스크림만으로는 파도 마루·모닥불처럼
밝은 사진 위에서 흰 글씨가 묻혔다. `rgba(8,7,14,.72)`, 배경음만 흐르는 첫 3초에는
문장이 없으므로 `:empty` 일 때 판을 지운다.

**3-1. 첫 판(blur + 요소째 딤)이 폰에서 더 나빴다 — 같은 날 두 번 고쳤다.**
데스크톱에서는 멀쩡했고 아이폰에서만 글씨가 통째로 사라졌다.

- **`backdrop-filter` 를 걷어냈다.** iOS Safari 는 이 속성을 **요소의 opacity 와 무관하게
  전체 강도로 그린다.** 딤(나레이션 종료 후)이 되면 판과 글씨만 흐려지고 블러는 100% 로
  남아, 밝은 파도 영상이 판 자리에 그대로 번져 글씨를 지웠다 — 화면에 밝고 뿌연 사각형만
  남은 정체가 이것이다. 불투명한 판 하나로 대비는 충분하고 밤새 도는 화면에서 GPU 도 덜 먹는다
- **딤에서 요소가 아니라 글자 색만 잦아들게 했다** (`opacity` → `color` 0.38).
  요소째 흐리면 어두운 판까지 투명해져서, 블러를 빼도 밝은 씬 위에서는 대비가 없다.
  판이 남으면 화면은 오히려 더 어두워지므로 원래 의도와도 맞는다
- **높이를 `svh` 로 가뒀다.** `max-height: 100%` 는 flex 로 정해진 높이를 백분율로 되받는
  모양이라 Safari 에서 풀려, 자막이 아래 버튼을 화면 밖으로 계속 밀어냈다 (데스크톱은 통과).
  `46svh` + `.screen { min-height: 0 }` 로 못 박았다. 글씨 영역이 크다는 지적도 같이 반영해
  19 → 18px · 여백 24 → 16px · 최대폭 26 → 24em 으로 줄였다
- 실측(375×812): 자막 높이 374 = 46svh · 14문장 최악에서도 그만두기 바닥 **798 < 812** ·
  페이지 넘침 없음 · `backdrop-filter: none` · 딤에서 판 유지된 채 글씨 읽힘

**4. 재생 화면 레이아웃 정리.** 자막은 `.play-body`(남는 자리 전부, 가운데 정렬),
알람 시각·슬라이더·버튼은 `.play-console` 한 덩어리로 묶어 간격을 하나로 통일했다.
**긴 조각에서 버튼이 잘리던 것**도 같이 고쳤다 — flex 아이템 기본 최소 높이가 내용 크기라
`flex:1` 만으로는 안 줄어든다. `min-height:0` + 자막 `max-height:100%; overflow-y:auto`
(스크롤 막대는 어둠 속에서 밝은 줄이 되므로 감춤). 실측: 12문장짜리 최악에서도
그만두기 버튼 바닥 798 < 뷰포트 812, 페이지 넘침 없음.

`node test.js` **31개** 통과 · 콘솔 에러 0 · `sw.js` VER 그대로(앱 파일은 네트워크 우선, mp3 무변경).

### 2026-09-01 — 호흡 모드 분리 (배포됨)

**재생 화면에서 호흡 게이지를 뺐다.** 대본에서 숫자 세기가 빠진 뒤로(2026-08-26)
말과 게이지를 이어줄 고리("하나"를 듣고 되감기)가 사라져 구조적으로 따로 놀았고,
화면을 보게 만드는 것 자체가 수면에 역행한다. 나레이션의 `breath` 층은 말로만 이끈다.

**홈에 "호흡" 화면(`s-breathe`)을 새로 만들었다.** 나레이션 없이 링과 숫자만 쓴다.

- 패턴 3종: **잠들기 전 4-7-8 · 불안할 때 상자 4-4-4-4 · 긴장 풀 때 4-6.**
  칩으로 고르고 `settings.breathPattern` 에 저장 — 재진입하면 마지막 패턴이 그대로다
- 숫자는 **남은 초 내려세기**(4→3→2→1). 올려 세기는 "1"이 0초에 떠서 실제보다
  빠르게 느껴졌다 — "카운트가 초가 아닌 것 같다"의 정체. 내려세기는 한 칸에 정확히 1초다
  (브라우저 실측: 1초 간격 스냅샷에서 4→3→2→1→다음 위상 7)
- 링은 재생 화면 때(184px)보다 큰 264px, 숫자 52px, 경과 시간(m:ss) 표시
- 게이지 엔진(`runBreath`)은 옛 `startBreathing` 을 패턴 인자로 일반화한 것 —
  링 한 바퀴 = 한 사이클, 위상 경계에서 아무 일도 안 일어나는 설계는 그대로다
- 화면을 떠나면 `stopBreathe()` 가 타이머를 전부 끈다 (`go()` 에서 일괄 처리)
- 모바일(375px) 확인: 가로 오버플로 없음. 콘솔 에러 0, `node test.js` 29개 통과
- CLAUDE.md 결정 표의 "호흡 정리는 breath 층이 이끈다" 행을 현행으로 고쳤다
- `sw.js` VER 은 안 올렸다 — 앱 파일은 네트워크 우선(`cache: 'reload'`)이라 배포 즉시
  갱신되고, mp3 는 안 바뀌었다 (VER 을 올리면 11MB 캐시를 전부 다시 받는다)

### 2026-08-29 — 슬라이더 50/50 정렬 · 밸런스 1.6배 · 홈 배경음 복구 (배포됨 2026-09-01)

- **목소리 슬라이더 상한 150 → 100** (index.html). 배경음(0~100)과 범위가 달라
  둘 다 50인데 손잡이 위치가 달랐다. 저장값이 1.0 을 넘으면 1.0 으로 이전한다.
- **배경음 전체 배율 0.15 → 0.24** (audio.js `_target`). "배경음 80 / 목소리 50 이
  맞는 밸런스"라는 귀 판정을 슬라이더 50/50 기본값에 옮긴 것 (0.24 = 0.15 × 1.6).
  실측: 기본 프리셋에서 waves el.volume **0.0432 = 0.6² × 0.24 × 0.5**.
- **`Mixer.unlock()` 이 재생 거부를 삼키던 것 수정.** 거부돼도 `unlocked=true` 로 남아
  openBed 가 성공으로 알고 첫 터치 리스너를 떼 버렸다 — "메인에서 배경음이 안 나와"의
  정체. 이제 거부는 던지고 다음 터치에서 재시도한다. `stopAll` 로 멎은 keepAlive 도
  unlock 재호출로 되살아난다. 호출부 5곳에 catch 추가.
- **홈으로 돌아오면 배경음이 다시 흐른다** — `go('home')` 에서 `startHomeBed()`.
  그만두기의 `fadeAllOut` 은 제거했다 (홈에서도 흘러야 하므로 끌 이유가 없다).
  실측: 그만두기 → 홈, stopAll(알람 종료 상황) → 기록 → 홈 둘 다 waves 0.0432 로 재개.
  프리셋을 장작 난로로 바꾸고 재진입하면 메인에서 fire 0.0461 이 나온다 (기억 동작).
- `window.__dbg`(mixer·store) 콘솔 실측 핸들 추가 — 오디오 엘리먼트가 DOM 밖이라
  이것 없이는 볼륨을 잴 수 없다.
- 검증: `node test.js` 29개 통과 · 콘솔 에러 0 · 위 브라우저 실측.

### 2026-08-26 (5) — 감속을 후처리에서 앵커로 옮겼다 · 볼륨 기본 50/50 (배포됨)

**앵커는 목소리만이 아니라 말 속도도 정한다.** 앵커를 1.25배로 늘려 놓고 뽑아 보니
결과가 **1.27배** 느려졌다 — 거의 그대로 따라간다.

그래서 `SLOW`(굽기 직전 시간축 늘리기)를 **껐다.** 늘리기에는 1.45배라는 천장이
있지만(그 위는 자음이 뭉개진다) 앵커를 통한 감속은 **가공이 아니라 생성**이라
음질 손해도 천장도 없다. 앵커에 **1.43배**를 걸어 0.139 초/글자를 얻었다.

사용자가 판을 넷 듣고 D를 골랐다.

| 판 | 초/글자 | 방식 |
|---|---|---|
| v11 (직전 배포) | 0.116 | 후처리 1.12배 |
| A | 0.124 | 후처리 1.20배 |
| B | 0.130 | 후처리 1.25배 — "괜찮긴 한데 살짝 빠르다" |
| **D (채택)** | **0.139** | **앵커 1.43배 · 가공 0** |

**느림은 말과 침묵 두 곳에서 온다.** 말 늦추기는 한 문장 *안*이 늘어지고,
`sentenceGap` 은 문장 *사이*가 벌어진다. 둘 다 올렸다 — 침묵 **4.2 → 5.0초**
(층 배수가 곱해져 실제로는 5.0~10.5초). 침묵은 앱이 실시간으로 넣는 값이라
음질 손해가 0이고 재생성도 필요 없다.

**볼륨 기본값 100/100 → 50/50.** 배경음 슬라이더가 천장(100)에 붙어 있어서
더 키울 수가 없었다. **50/50은 100/100과 같은 균형이다** — 배경음은 `master`,
목소리는 `el.volume` 에 각각 선형으로 걸리므로 같이 반으로 줄여도 비율이 그대로다.
이전(migration)에는 표시(`volMigrated`)를 남겼다. 100은 사용자가 일부러 고를 수
있는 값이라, 값만 보고 옮기면 다음 실행에서 그 선택을 또 되돌린다. 브라우저에서
두 경우 다 확인했다 — 옛 100/100은 옮겨지고, 이후 일부러 올린 100은 유지된다.

**전량 검증** (790개)

| | 값 |
|---|---|
| 매니페스트 = 디스크 | 790 = 790 · 없는 파일 0 · 고아 0 |
| 내부 침묵 ≥0.4초 | 2개 (0.3%) · ≥0.7초 0 · ≥1.0초 0 |
| 초/글자 | 중앙 **0.140** · 5~95% 0.120~0.160 |
| 조각 사이 속도 | 0.117~0.160 = 1.38배 |
| 발화 RMS | 중앙 0.042 · 최대/최소 2.5배 |
| f0 | 중앙 113Hz · 5~95% 105~126Hz |
| 덩어리 | 81조각 중 **77개가 1덩어리** · 보정 8군데 |

`test_cut.py` 4개 · `test.js` 29개 통과. `sw.js` VER **v11 → v12**.

**더 느리게 하려면 `SLOW` 가 아니라 `tools/anchor/voice.wav` 를 늘린다.**
단, 지금 파일을 또 늘리면 흔적이 쌓이므로 **원본에서 다시 뜬다**
(`racing/breath/b1-3.mp3` → `tools/anchor/README.md` 절차).
앵커를 많이 늘릴수록 조각 사이 음색이 조금씩 벌어진다 (1.25배에서 14~21,
1.43배에서 16~39). 1.43배까지는 사용자 승인으로 확인됐다.

### 2026-08-26 (4) — 목소리를 파일 하나로 못 박았다 · 전량 재생성 (배포됨)

**"조각마다 목소리가 다르다"는 지적이 맞았다. 설정 문제가 아니었다.**

층별로 잘라 들려주니 정착·이완만 "이상하다"고 짚었고, 재 보니 그 둘이 정확히
음색이 가장 먼 둘이었다 (호흡을 0으로 둔 MFCC 거리: 정착 67.4 · 이완 61.4 ·
심상 41.3 · 소실 35.9). **음량과 속도를 다 맞춰 놓고도 남는 차이였다.**

원인은 `DESIGN` — 목소리를 **말로 된 묘사**로 만들고 있었다. 조각마다
`model.generate()` 를 새로 부르고 그때마다 묘사에서 목소리를 새로 뽑는다.
**시드 42 고정으로는 안 막힌다.**

**`tools/anchor/voice.wav` 를 만들어 모든 생성에 물렸다.** 사용자가 고른 그 목소리
(`racing/breath/b1` 의 네 번째 토막, 3.16초)를 `prompt_wav_path`+`prompt_text` 로
넘긴다. 앵커가 있으면 `DESIGN` 은 붙이지 않는다 — 목소리는 앵커가 정하고,
묘사를 같이 주면 둘이 싸운다. 만드는 법은 `tools/anchor/README.md`.

**앵커가 생성 자체를 안정시켰다.** 목소리만 잡은 게 아니다.

| racing 15조각 | 앵커 전 | 앵커 후 |
|---|---|---|
| 덩어리 1개로 나온 조각 | 9/15 | **14/15** |
| 속도 보정 | 8군데 | **0군데** |

모델이 문장 사이에서 제대로 쉬니 자르기가 한 번에 성공한다. 시간축 가공이
거의 사라져서 쇳소리 위험도 같이 없어졌다. 전체 81조각 중 **72개가 덩어리 1개·보정 0**.

**전량 검증** (790개)

| | 값 |
|---|---|
| 매니페스트 = 디스크 | 790 = 790 · 없는 파일 0 · 고아 0 |
| 내부 침묵 ≥0.4초 | **1개 (0.1%)** — 자르기 고치기 전 28.1%, 직전 판 9.0% |
| ≥0.7초 / ≥1.0초 | **0개 / 0개** |
| 발화 RMS | 중앙 0.043 · 최대/최소 **2.3배** (직전 16.5배) |
| 초/글자 | 중앙 0.116 · 5~95% 0.103~0.131 |
| f0 | 중앙 115Hz · 5~95% 106~129Hz |
| 대사에 남은 숫자 세기 | 0개 |

`test_cut.py` 4개 · `test.js` 29개 통과. `sw.js` VER **v10 → v11**.

**되돌린 것 두 가지 — 기록해 둔다. 다시 넣지 말 것.**

- **토막별 음량 맞추기(`NORM_RMS`)** → "평평하다". 목표를 0.11 로 높게 잡아
  사용자가 좋아한 작고 부드러운 호흡을 3배 키운 것이 문제였다. 지금은 껐고,
  대신 **조각 통째로 한 배수**(`PIECE_RMS` 0.045)만 걸어 조각 *사이*만 맞춘다
- **전체 공통 속도 목표** → "어색하다". 조각을 통째로 최대 1.33배까지 밀게 되고
  그게 말투 자체를 바꾼다. 속도는 조각별 중앙값 기준(`PACE_BAND` 1.25)으로 되돌렸다

억양은 토막 사이·조각 사이의 **차이**에서 나온다. 고르게 만들수록 사람이 아니게 된다.

**새 인트로도 같이 나갔다.** `settle` 21개 조각을 다시 썼다 —
예고 한 문장 → 몸이 닿은 자리 → 하루를 닫기 → 허락 → 자세.
`awoken` 만 길이를 안 늘렸다 (정의 5절 "다시 깨우면 안 된다, ~90초").
`rewrite_states.py`(정의)와 `narration.json`(산출물) 양쪽을 맞췄다.

### 2026-08-26 (3) — 배경음을 누르지 않는다 · 목소리는 전부 같은 판이었다 (배포됨)

**순서가 거꾸로였다.** 배경음 크기는 사용자가 준비 화면에서 정하는 값인데, 앱이 나레이션
중에 그걸 다시 눌러 버리면 사용자가 정한 게 무의미해진다. 맞춰야 하는 쪽은 목소리다.
(2)에서 나레이션을 RMS 0.11 로 키워 놨으므로 이제 누를 이유가 없다.

- `Mixer.DUCK = 1` — **나레이션 중에도 배경음을 그대로 둔다.** 상수로 빼서 목소리가
  묻히면 0.6 → 0.4 로 내릴 수 있게 남겼다 (귀로만 판정되는 값이라 지우지 않았다)
- 균형: 배경음을 최대로 올려도(레이어 1.0·master 1.0) 실효 RMS 0.0162 인데 목소리는
  0.11 이라 **6.8배 위**다. 기본 프리셋(파도 0.6)이면 19배

**배경음이 홈 화면부터 나오는지 — 브라우저에서 실측했다. 나온다.**
첫 터치 뒤 레이어 3개가 재생되고 볼륨 0.054(파도)·0.0086(바람)까지 확인했다.
브라우저는 제스처 없이 재생을 못 하므로 **첫 터치 전까지는 어떤 방법으로도 소리가 안 난다.**
상태 카드를 첫 터치로 누르면 배경음이 켜지는 순간 화면이 넘어가서 홈에서 못 듣게 된다.

- `openBed` 의 `once: true` 를 걷어냈다. 그 한 번의 터치에서 `unlock()` 이 거부되면
  리스너가 사라져 **그 세션 내내 배경음이 없었다.** 이제 성공할 때까지 다시 건다

**"목소리 설정이 일부만 반영된 것 아니냐" — 아니다. 731개 전부 같은 판이다.**

| 상태 | racing | anxious | wired | unknown | awoken | calm | nap |
|---|---|---|---|---|---|---|---|
| f0 중앙값 | 107Hz | 105Hz | 103Hz | 107Hz | 101Hz | 104Hz | 104Hz |

7개 상태가 101~107Hz 로 모여 있다. 다른 목소리가 섞였다면 상태별로 갈렸을 것이다.
음색(MFCC) 거리로 봐도 특정 상태에 몰린 무리가 없다. 파일 전체 f0 가 90~132Hz 인 것은
**한 사람의 정상적인 억양 범위**다. (2)에서 "음높이가 조각 안에서 30Hz 흔들린다"고
적어 둔 것도 같은 이야기 — 결함이 아니라 억양이다.

### 2026-08-26 (2) — 배경음이 나레이션 중에 안 들리던 것 + 음량 균일화 (배포됨)

**두 지적("배경음이 안 나온다", "목소리가 중간에 바뀐다")은 뿌리가 같았다.**
나레이션 mp3 의 음량이 제각각이라 — **발화 RMS 가 파일 사이 16.5배**, 한 조각 안에서도
3.8배가 흔들렸다 — 배경음을 0.08 까지 눌러야 목소리가 들렸고, 그 0.08 이 배경음을 지웠다.

- `tts_voxcpm.normalize()` — 굽기 직전에 발화 RMS 를 `NORM_RMS`(0.11) 로 맞춘다.
  **침묵을 뺀 구간으로 잰다** (파일 전체로 재면 침묵 많은 토막이 과하게 커진다).
  피크는 `PEAK_CEIL`(0.95) 로 눌러, 못 채우는 파일은 덜 키운 채로 둔다
- `reslow.py --normalize` — 이미 있는 731개에 같은 기준 적용. 음량 맞추기는 목표를 향해
  수렴하므로 여러 번 돌려도 누적되지 않는다 (그래서 속도 안전장치를 건너뛴다)
- `audio.js _target()` — 덕킹 **0.08 → 0.20**. 목소리가 1.9배 커졌으니 배경도 올릴 수 있다.
  **이 둘은 같이 움직여야 한다** — `NORM_RMS` 를 바꾸면 덕킹도 같이 봐야 한다

| | 전 | 후 |
|---|---|---|
| 발화 RMS 최대/최소 | 16.5배 | **3.1배** |
| RMS 5~95% 범위 | 0.031~0.135 | **0.083~0.105** |
| 나레이션 중 배경음 | 0.012 | **0.030** (덕킹 −14dB, 브라우저 실측) |
| 조각 안 RMS 3배 초과 | 3개 | **1개** |

**아직 안 풀린 것 — 음높이.** 조각 안에서 f0 가 30Hz 넘게 흔들리는 조각이 **16/81** 이다.
처음엔 "한 조각을 여러 번 나눠 생성해서"라고 봤는데 **틀렸다** — `racing/release/r1` 은
**덩어리 1개인데도 35Hz** 가 흔들린다. 즉 긴 대사를 한 번에 읽어도 모델이 자연스럽게
높낮이를 바꾼다. 사람 억양 범위이기도 해서, 이게 실제로 "다른 목소리"로 들리는지는
**음량을 고친 지금 다시 들어봐야 판단이 선다.** 음량(16.5배)이 훨씬 큰 변수였다.

### 2026-08-26 — 숫자 세기 제거 · 12% 감속 · 쇳소리 원인 제거 (배포됨)

들어보고 나온 지적 넷을 순서대로 잡았다. **전부 귀로 먼저 걸렸고, 그다음에 수치로 확인됐다.**

**1. 말이 빠르다 → 1.12배로 늘린다.** 모델은 초당 6.9음절로 말한다. 명상에 쓰기엔 빠르다.
1.25배는 "너무 느리다", 1.0은 "빠르다" 였고 **1.12배**(초당 6.1음절)로 정해졌다.
`tts_voxcpm.py` 의 `SLOW` 로 굽기 직전에 늘린다. 침묵이 아니라 말을 늘린 것은
이번이 처음이다 — 그동안은 "느림은 침묵에서" 였는데, 침묵을 아무리 늘려도
말 자체가 빠르다는 인상은 안 없어졌다.

**2. 쇳소리 → `librosa` 를 걷어냈다.** `equalize()` 가 속도 튀는 토막을
`librosa.effects.time_stretch` 로 늘렸는데 **위상 보코더라 사람 목소리에서 금속음이 난다.**
`tempo()` 를 새로 두고 **ffmpeg `atempo`(시간영역 WSOLA)** 로 바꿨다.
`tts_voxcpm.py` 에 `time_stretch` 호출은 이제 없다 (`librosa` 는 무음 구간 탐지에만 쓴다).
배포본에 이 방식으로 늘려진 토막이 22개 있었다.

**3. "그리고 **어** 더 길게 내쉽니다" → 대사에 없는 발성이었다.**
`racing/b1-6` 은 대사가 11음절인데 소리 덩어리가 3개였다 —
앞(`그리고` 0.16초/음절)·뒤(`더 길게 내쉽니다` 0.16초/음절)는 정확한데
**가운데 0.51초가 대사에 없다.** 양옆 간격이 0.61·0.51초라 `MAX_INNER_SEC`(0.6초)를
아슬아슬하게 통과했다. **시드가 42 고정이라 다시 뽑아도 같은 게 나온다** —
대사가 바뀌어야 결과가 바뀐다. `그리고 더 길게` → `이번에는 더 길게` 로 고쳤고,
새 파일은 덩어리 2개(간격 0.22초)로 고립된 발성이 없다.

**4. 숫자 세기(하나·둘·셋) 전량 제거.** 숫자 토막은 음절당 0.277초로 문장(0.149초)의
**1.9배로 늘어졌고 음량은 최대 5배 작았다.** 작고 숨 섞인 소리로 흘러 앓는 소리처럼 들렸다.
`하나` 8개 중 7개가 중심주파수 1030~1320Hz(다른 숫자는 456~607Hz)로 특히 나빴다.
당기면 경박해지고 그대로 두면 신음이라, 대사에서 아예 뺐다.
호흡비는 말로 지시한다 — `짧게 들이쉽니다. 그보다 길게 내쉽니다.`
**`rewrite_states.py`(정의)와 `narration.json`(산출물) 양쪽을 같이 고쳤다** —
한쪽만 고치면 다음에 정의 파일을 돌리는 순간 숫자가 되살아난다.
바뀐 조각은 5개(`racing/b1`·`anxious/b1`·`wired/b2`·`unknown/b1`·`calm/b1`).

**5. 자막을 목소리보다 2초 먼저 띄운다** (`audio.js`). 조각 사이 침묵에서 빼 오므로
전체 길이는 그대로다(간격 6초 = 침묵 4초 + 리드 2초). 브라우저에서 실측 확인:
자막 0ms → 목소리 2014ms, 자막 6330ms → 목소리 8342ms.

**`tools/reslow.py` 신규.** 감속을 정한 시점에 mp3 가 이미 다 만들어져 있었다.
모델부터 다시 돌리면 두 시간인데 늘리는 것만이면 몇 분이다. **두 번 돌리면 두 번 늘어나므로
굽기 전에 현재 속도를 재서 이미 느려져 있으면 멈춘다** (실제로 재실행이 차단되는 것까지 확인).
앞으로 새로 뽑는 파일은 `tts_voxcpm.py` 가 알아서 늘려 굽는다.

**검증** (숫자가 빠져 파일이 788 → 731개가 됐다)

| | 자르기 수정 전 | 지금 |
|---|---|---|
| 내부 침묵 ≥ 0.4초 | 28.1% | **9.0%** |
| ≥ 0.7초 | 10.0% | **1.0%** |
| ≥ 1.0초 | 10개 | **0개** |
| 발화 초/글자 편차 | 2.80배 | **2.01배** |
| 말 속도 | 0.152초/음절 | **0.162초/음절** (숫자 제외 재집계) |

`tools/test_cut.py` 4개 · `node test.js` 29개 통과. 매니페스트 731개 = 디스크 731개, 고아 파일 0.
`sw.js` VER **v7 → v8**.

**남은 것**: `unknown/b1-4`(`한 번 더 들이쉬고, 길게 내쉽니다.`) 안에 0.96초 침묵이 있다.
쉼표 자리라 부자연스럽진 않고 한 파일 안이라 앱이 침묵을 더 넣지도 않는다. 거슬리면 그 문장만 손본다.

### 2026-08-25 (7) — 문장이 반토막 나던 원인: 자르기 검증이 뚫려 있었다 (7개 상태 전량 재생성 · 배포됨)

**증상.** 나레이션이 문장을 못 끝내고 뚝 끊긴 뒤 몇 초 침묵이 오는 자리가 많았다.

**원인은 `cut()` 과 `check()` 가 같은 것을 본다는 데 있었다.** `cut()` 은 경계를
**길이 비례**만으로 고르고 그 자리에 모델이 실제로 쉬었는지는 안 봤다. 그런데 `check()` 도
같은 길이 비례를 본다 — DP 가 이미 최소화해 놓은 값이라 **잘못 잘린 판은 구조적으로 통과한다.**
게다가 `equalize()` 가 파일을 내부 침묵까지 통째로 압축해 초/글자를 맞추는 바람에,
남의 문장 조각을 문 파일이 오히려 "정상 속도"로 보였다. 3차·4차 검증이 깨끗했던 이유가 이것이다.

**실측 (배포본 788개 전수)** — 한 파일 = 짧은 문장 하나인데 그 안에 긴 침묵이 있음:

| 내부 침묵 | 파일 |
|---|---|
| ≥ 0.4초 | 206개 (26%) |
| ≥ 0.5초 | 149개 (19%) |
| ≥ 0.7초 | 73개 (9%) |

149개의 침묵 위치는 **끝쪽 54개 · 앞쪽 13개** — 경계가 밀려 옆 문장을 물었다는 지문이다.
모델을 다시 돌려 자르기 과정을 직접 본 결과도 같았다: `racing/settle/s3` 은 실제 쉼이
중앙값 0.165초(최대 0.789초)인데 경계는 **중앙값 0.021초 자리**에 찍혔고 검사를 통과했다.
`racing/release/r2` 는 경계 15개가 전부 0.25초 미만 자리였다.

**고친 것 (`tools/tts_voxcpm.py`)**

- `cut()` — DP 안에서 경계 후보를 거른다. `MIN_GAP_SEC`(0.15초와 실쉼 중앙값의 절반 중 큰 쪽)
  미만인 자리는 경계로 못 쓰고, 조각 안에 `MAX_INNER_SEC`(0.6초)를 넘는 침묵이 남아도 안 된다.
  못 자르면 `None` → **기존 "반씩 쪼개 다시 뽑기" 경로**가 받는다. 새 경로는 안 만들었다
- `voiced()` 추가. `spread()`·`equalize()` 가 파일 길이가 아니라 **침묵 뺀 발화 시간**으로 잰다.
  안 그러면 이 버그가 또 측정에서 숨는다
- `tools/test_cut.py` 신규. 길이 비례가 **일부러 틀린 답을 가리키게** 만든 파형으로 4가지를 본다 —
  긴 쉼을 고르는가 / 쉼이 없으면 물러나는가 / 조각 안에 긴 침묵이 남으면 물러나는가 / 한 문장은 통짜인가.
  `tools\.venv-voxcpm\Scripts\python.exe tools\test_cut.py` → 4개 통과

**racing 재생성 결과** (157개 비교. 옛 파일은 `audio/_backup-racing-old/` 에 보관)

| | 전 | 후 |
|---|---|---|
| 내부 침묵 ≥ 0.4초 | 34개 (21.7%) | **14개 (8.9%)** |
| ≥ 0.7초 | 16개 (10.2%) | **2개 (1.3%)** |
| ≥ 1.0초 | 3개 | **0개** |
| 발화 초/글자 편차 | 2.52배 | **1.95배** |

통짜로 못 뽑아 쪼갠 조각은 6개(`b3` 7덩어리, `b1` 5덩어리 …). 쉼이 모자란 자리를 억지로
자르는 대신 물러난 결과라 의도대로다. 조각 내 편차는 전부 거부선(2.06배) 안이다.

racing 미리듣기를 듣고 승인받아 **나머지 6개 상태도 같은 방식으로 전량 재생성**했다.

**전체 결과** (숫자 세기를 뺀 732개. 옛 파일은 `audio/_backup-racing-old/`·`audio/_backup-old-all/`)

| | 전 | 후 |
|---|---|---|
| 내부 침묵 ≥ 0.4초 | 206개 (28.1%) | **60개 (8.2%)** |
| ≥ 0.5초 | 149개 (20.4%) | **26개 (3.6%)** |
| ≥ 0.7초 | 73개 (10.0%) | **6개 (0.8%)** |
| ≥ 1.0초 | 10개 | **0개** |
| 발화 초/글자 편차 | 2.80배 | **2.04배** |
| 중앙값의 0.7배 미만(내용 잘린 토막) | 5개 | **0개** |

통짜로 못 뽑아 쪼갠 조각은 7개 상태 합쳐 26개. 쉼이 모자란 자리를 억지로 자르는 대신
물러난 결과라 의도대로고, **조각 내 편차는 7개 상태 전부 거부선(2.06배) 안**이다.

남은 6개(≥0.7초)는 자르기 탓이 아니라 **모델이 문장 안에서 스스로 쉰 것**이다
(`breath/b3` 계열에 몰려 있다). 이건 잘라서 못 고치고, 거슬리면 굽는 단계에서
긴 내부 침묵을 줄이는 수가 있다 — 실사용에서 판단할 것.

**검증**: `tools/test_cut.py` 4개 통과 · `node test.js` 29개 통과 · mp3 788개 11MB.
`data/narration.json` 은 경로가 그대로라 변경 없음. `sw.js` VER **v6 → v7**.

### 2026-08-25 (6) — 재생 중 배경음·목소리 조절 (배포됨)

누운 뒤에 다시 일어나 준비 화면으로 돌아가게 만들 수는 없다. 재생 화면에 슬라이더 둘을 넣었다.

- **배경음** — `Mixer.master`. 레이어 사이의 균형은 그대로 두고 전체가 같이 오르내린다.
  준비 화면의 레이어별 슬라이더가 "무엇을 얼마나 섞을지"라면 이건 "전체를 얼마나"다
- **목소리** — 0~150%. 나레이션 파일은 피크가 0.15~0.40 밖에 안 돼서 1.0 을 넘겨 키울 여지가 있다.
  `el.volume` 은 1 이 천장이라 **1 을 넘기면 파형에 굽는다** — 아이폰이 아니어도 마찬가지다
- 목소리는 **다음 토막부터** 새 크기로 나온다. 말하는 도중에 소리가 변하는 것보다 낫고
  침묵이 4초씩 있어서 금방 넘어간다
- 둘 다 `store.settings`(`bedVolume`·`narrationVolume`)에 저장돼 다음 밤에도 유지된다
- 딤 상태에서 같이 흐려진다 (`#app.is-dim .play-mix`)

**검증** — 로컬 실측.

| | 실측 | 기대 |
|---|---|---|
| 배경음 100 (덕킹 중) | 0.0043 | 0.0043 |
| 배경음 40 (덕킹 중) | 0.0017 | 0.0017 |
| 목소리 100 | 원본 그대로 재생(굽지 않음) | — |
| 목소리 50 · 아이폰 | RMS 0.01662 · 구움 | 0.01664 |
| 목소리 150 · 아이폰 | RMS 0.04989 · 구움 | 0.04991 |
| 목소리 50 · 일반 기기 | `el.volume` 0.5 (굽지 않음 — 그쪽이 싸다) | — |

`node test.js` 29개 통과 · 콘솔 에러 없음.

### 2026-08-25 (5) — 아이폰 감지가 틀려서 (4)가 안 켜지고 있었다 + 알람도 굽기 (배포됨)

**(4) 의 굽기 경로가 아이폰에서 아예 안 켜졌다.** 감지를 이렇게 했는데 —

```js
const a = new Audio(); a.volume = 0.5;
return Math.abs(a.volume - 0.5) < 0.01;   // 되읽히면 지원됨
```

**iOS 는 대입을 무시하면서 값은 되돌려 준다.** 그래서 이 검사가 `0.5` 를 받고
"지원됨"으로 통과해 버렸다. 데스크톱에서 `volume` 을 읽기 전용으로 덮어쓴 시뮬레이션은
당연히 통과했고(그건 안 되돌려 주니까), 실기기에서만 안 켜졌다.
**흉내가 실제와 다른 지점을 못 본 것이다.** iPhone UA 로 위장해 다시 재고서야 잡혔다.

- `IS_IOS` 를 추가해 **기기 판별을 먼저** 하고, 그다음에 되읽기를 본다
  (아이패드는 데스크톱 사파리로 위장하므로 `maxTouchPoints` 로 걸러낸다)
- **설정 화면에 현재 볼륨 방식을 표시한다** (`#audio-mode`). 눈으로 못 보면 또 추측으로 고치게 된다
- `sw.js` VER v5 → **v6** — 낡은 앱 파일이 캐시에 남지 않게

**알람도 같은 경로로.** `createAlarm` 이 `new Audio()` 대신 `SeamlessLoop` 을 쓴다.
아이폰에서는 60초 램프도 죽어 있어서 처음부터 최대로 울고 있었다.
`_bake` 에 **최대 대기(`BAKE_MAX` 4초)** 를 넣었다 — 디바운스만으로는 1분 내내 값이 움직이는
알람 램프가 영영 안 구워지고 끝에서 한 번에 커진다.
**설정에 `알람 들어보기` 버튼**을 넣었다(램프 12초로 압축). 밤새 기다려야만 들을 수 있으면
고쳤는지 확인할 방법이 없다.

**검증** (iPhone UA 로 위장 + 생성된 WAV 를 디코드해 RMS 측정)

| | 실측 | 기대 |
|---|---|---|
| `volume` 되읽기 | **0.5 를 돌려준다** | ← 옛 감지가 속은 지점 |
| 슬라이더 50 | 0.00404 | 0.00405 |
| 덕킹 중 | 0.000312 | 0.000324 |
| 덕킹 해제 | 0.004037 | 0.00405 |
| 슬라이더 15 | 0.000353 (원본의 **1/306**) | 0.000364 |

알람 램프 굽기 단계(20초로 압축): 0.150 → 0.320 → 0.490 → 0.660 → 0.830 → 1.000.
일반 기기 경로는 그대로 — 알람이 12초에 걸쳐 0.288 → 1.000 으로 매끄럽게 오른다.
`node test.js` 29개 통과.

### 2026-08-25 (4) — 아이폰에서 볼륨이 아예 안 먹던 것 (배포됨)

**이 앱은 아이폰에서 믹서가 통째로 죽어 있었다.**
iOS Safari 는 `HTMLMediaElement.volume` 이 **읽기 전용**이다 — 대입해도 예외 없이 조용히
무시되고 항상 1 이다. 이 믹서는 전부 `el.volume` 위에 지어져 있어서 아이폰에서는
**슬라이더도 덕킹도 하나도 듣지 않았다.** 배경음이 늘 최대치로 나오고 나레이션(RMS 0.034 짜리
속삭임)이 그 밑에 깔렸다. "최소로 해도 너무너무 크고 목소리가 안 들린다"가 정확히 이것이다.

세 번에 걸쳐 마스터를 0.55 → 0.35 → 0.30 으로 내렸는데 아무 변화가 없던 이유도 같다.
숫자는 맞았고 기기에서 무시되고 있었다. **데스크톱에서 재는 것만으로는 못 잡는 종류의 버그다.**

- `synth.js` `wavBytes(bed, gain)` — 인코딩할 때 샘플에 볼륨을 곱한다
- `audio.js` `VOLUME_SETTABLE` — 기기가 볼륨을 받는지 한 번 검사한다
  (`new Audio()` 에 0.5 를 넣고 되읽어 본다)
- 안 받는 기기에서는 `SeamlessLoop._bake()` 가 **볼륨을 파형에 구워** 소스를 갈아 끼운다.
  같은 `currentTime` 으로 이어 붙이므로 흐름은 유지된다. `fade()` 가 50ms 마다 쓰므로
  150ms 디바운스로 마지막 값만 굽는다 — 그 기기에서 페이드는 계단 하나가 된다
- DSP 결과는 `pcmCache` 에 종류당 한 번만 담는다. 볼륨이 바뀌면 **다시 인코딩만** 한다
  (렌더 200~400ms vs 인코딩 수십 ms)
- 볼륨이 안 먹는 기기에서는 `_tick()` 의 겹치기 넘김을 끈다 —
  둘 다 최대치로 나와서 1.2초 동안 소리가 **두 배**가 되기 때문이다. `el.loop` 안전망에 맡긴다
- Web Audio GainNode 로 가면 될 일이지만 그 길은 막혀 있다 —
  화면을 끄면 iOS 가 AudioContext 를 정지시킨다 (CLAUDE.md 첫 번째 결정)

**같이 조정한 것** — 마스터 0.30 → **0.15**("지금의 절반이면 되겠다") ·
프리셋 `쏟아지는 비` 를 0.85/0.34 → 0.6/0.24 로 낮춰 슬라이더가 전부 0.6 이하에서 시작하게 했다
(나머지 프리셋은 이미 0.3~0.62 라 그대로 뒀다).

**검증** — `volume` 을 읽기 전용으로 덮어써 아이폰을 흉내 낸 뒤 실제로 생성된 WAV 를 디코드해 RMS 를 쟀다.

| | 실측 RMS | 기대값 |
|---|---|---|
| 슬라이더 50 | 0.00403 | 0.00405 |
| 덕킹 중 | 0.00031 | 0.00032 |
| 슬라이더 20 | 0.00004 | 0.00005 |

일반 기기 경로도 그대로다 — 배경 `0.0043`(= 0.6²×0.15×0.08), 나레이션 `1.0000`.
`node test.js` 29개 통과.

**아직 남은 것** — 알람(`createAlarm`)의 60초 램프도 `el.volume` 을 쓴다.
아이폰에서는 처음부터 최대로 운다. 깨우는 소리라 위험하지는 않지만 설계와는 다르다.

### 2026-08-25 (3) — 남은 2조각까지 속도 균일화 (배포됨)

3차 뒤에도 `racing/release/r1`·`wired/release/r1` 둘이 거부선(2.06배)을 넘어 있었다
(2.17 / 2.38배). 16문장짜리 몸 훑기라 **한 덩어리 안에서 속도가 앞뒤로 흐르는** 경우다 —
0.229초/글자로 시작해 0.106 으로 끝난다. 보정 폭(33%)으로는 못 따라잡고, 반으로 한 번
쪼개는 것도 부족했다 (그대로 2.17 / 2.38).

- `tools/tts_voxcpm.py` 에 **점진 분할**을 넣었다. 편차가 거부선을 넘으면 분할 단계를
  올려가며(`MAX_SPLIT 3`) 다시 뽑고, **나아질 때만** 그 판으로 바꾼다.
  실측 racing 2단계 1.63배 · wired 3단계 1.94배
- 그 2조각(mp3 32개)만 다시 뽑았다. 나머지 749개는 3차 그대로다
- `sw.js` VER v4 → **v5** (v4 가 이미 배포돼서 안 올리면 옛 r1 파일이 캐시에서 계속 나온다)
- 검증: `node test.js` 29개 통과 · mp3 에서 직접 잰 편차 중앙값 1.54배·최대 1.94배 ·
  **거부선 초과 0개** · 파일 788개 전부 존재 · 문장수 불일치 0

### 2026-08-25 (2) — 나레이션 3차 재생성 · 배경음 재조정 · 슬라이더 잠김 수정 (배포됨)

**1. 나레이션 417개 재생성.** `tools/tts_voxcpm.py` 를 고쳐 다시 뽑았다.
자를 수 없는 조각을 곧장 문장별 생성으로 내리는 대신 **반씩 쪼개 통짜로 다시 뽑고**(재귀 분할),
마지막에 튀는 토막만 시간축으로 당긴다(`PACE_BAND 1.25` · `STRETCH_MIN 0.75`).
문장별 생성이 속도 편차의 주범이었다는 판단이다. 매니페스트(`data/narration.json`)는 안 바뀌었다 —
조각당 파일 개수가 그대로라 앱 쪽 정렬은 영향이 없다.
`sw.js` VER v3 → **v4** (mp3 는 캐시 우선이라 안 올리면 옛 목소리가 그대로 나온다).

**2. "배경음이 아직도 목소리를 덮는다" (두 번째 피드백).** 원인을 재서 찾았다 —
숫자가 아니라 **소리 크기 자체가 문제**였다.

| | RMS | 피크 |
|---|---|---|
| 나레이션 mp3 (racing/settle 6개 실측) | 0.027 ~ 0.109 (중앙값 0.034) | 0.15 ~ 0.40 |
| 합성 배경음 | 0.108 (전 종류 통일) | ~0.5 |

VoxCPM 목소리는 속삭임이라 **같은 볼륨이면 배경의 1/3**이다. 게다가 광대역 소음은
속삭임을 RMS 비율보다 훨씬 심하게 덮는다(말은 짧은 봉우리에 에너지가 몰려 있고 소음은 안 끊긴다).
- 마스터 0.35 → **0.30**, 덕킹 0.22 → **0.08**
- 나레이션 볼륨 기본값 0.9 → **1.0** (파일 피크가 0.40 이라 클리핑 여지가 없다.
  저장된 설정이 덮으므로 옛 기본값 0.9 인 사람만 옮긴다 — `sentenceGap` 과 같은 방식)
- 실측: 배경 0.1080 → 덕킹 시 **0.0086**, 나레이션 **1.0000**.
  실효 RMS 비 10배 → **31배(30dB)**. 가장 작은 나레이션 토막으로 쳐도 25배

**3. 프리셋에 든 사운드만 슬라이더가 잠겼다.** `setVolume` 이 `play()` 거부를 전부
"음원 없음"으로 판정해 `available=false` 로 내리고 있었다. 프리셋은 자기 레이어만 재생하니
딱 그것들만 잠긴다 — "포함 안 된 것만 조절된다"의 정체.
`AbortError`(볼륨을 0 으로 내렸다가 바로 올릴 때)·`NotAllowedError`(자동재생 미해제)는
소리가 없다는 뜻이 아니다. **`NotSupportedError` 일 때만** 음원 없음으로 본다.
※ 이 환경에서 거부 자체를 재현하지는 못했다 — iOS 처럼 제스처 밖 `play()` 가 막히는 기기에서
나는 것으로 보인다. 판정 로직이 틀린 것은 확실해서 그것을 고쳤다.

**검증** — `node test.js` 29개 통과 · 순수 모듈 4개 node 로드 OK ·
매니페스트 참조 788개 전부 존재, 디스크 788개, 깨진 파일 0 · 콘솔 에러 없음.

### 2026-08-25 — 나레이션 음성 VoxCPM2 로 전면 교체 (배포됨)

- **Google Chirp3-HD → VoxCPM2.** 배경과 확정값은 위 '음성 엔진 교체' 절. 788조각 · 11.0MB
- 새 생성기 `tools/tts_voxcpm.py`. `tts.py` 와 합치지 않았다 — venv 가 다르고(torch+CUDA,
  `tools/.venv-voxcpm`) 생성 단위가 다르다(문장 하나 vs 조각 통짜)
- 비교용 스크립트: `sample_voices.py`(Chirp3-HD 남성 16개), `sample_voxcpm.py`(voice design),
  `preview_voxcpm.py`(자르기 검증), `preview_full.py`(한 상태 통째 미리듣기)
- `sw.js` VER v1 → v2 → **v3**. 1차 배포(4개 상태)와 2차 배포(전체) 각각 올렸다
- `app.js` 저장된 `sentenceGap` 이 새 기본값을 덮는 문제 — 옛 기본값(3.6)일 때만 4.2 로 옮긴다
- `index.html` 설정 화면 표시 초기값 2.4초 → 4.2초 (렌더 전 잠깐 보이던 낡은 숫자)
- `.gitignore` `tools/.venv/` → `tools/.venv*/` (10GB venv 가 배포 저장소에 들어가지 않게)
- 검증: `node test.js` 29개 통과 · 매니페스트 파일 참조 788개 전부 존재 · 문장수 불일치 0

### 2026-08-24 — 볼륨 · 시작 순서 · 이음매 · 일시정지 · 소리 3종 · 씬 3종 (배포됨 → 287f559)

실사용 피드백 8건 + 검증 중 발견 1건을 한 세션에 처리했다.

**1. 배경음이 나레이션을 덮었다.** `audio.js` `_target()` 의 마스터를 0.55 → **0.35**,
덕킹을 0.45 → **0.22** 로 내렸다. 나레이션 중 배경 실효 볼륨이 0.089 → 0.028
(`waves 0.6` 기준). 목소리 대 배경 비율이 10:1 → 32:1.

**2. 배경음이 세션을 시작해야 나왔다.** `init()` 에서 사운드 레이어를 미리 등록하고
첫 `pointerdown`/`keydown` 에 한 번만 붙는 리스너로 배경음을 연다 — **홈 화면부터 소리가 흐른다.**
자동재생 정책 때문에 제스처는 필요하지만, 사용자가 화면을 한 번 만지는 그 순간이다.

**3. 호흡 정리를 게이지 혼자 하고 있었다.** 처음엔 3초 배경음 → 무음 게이지 24초 → 나레이션으로
짰는데, "호흡 정리는 나레이션이 하는 걸로"가 실제 요구였다. 그래서
**배경음 3초 → 나레이션 시작 → `breath` 층이 뜨는 순간 게이지 등장** 으로 바꿨다.
대본이 이미 "넷을 세며 들이쉽니다 · 하나, 둘, 셋, 넷"을 갖고 있어서 게이지는 반주만 하면 된다.
`breath` 층이 없는 상태(`awoken`·`nap`)는 첫 조각부터 게이지를 띄운다.

**4. 배경음이 가끔 "뚝" 끊겼다 다시 시작했다.** 파형 이음매는 이미 크로스페이드돼 있었으므로
(`synth.renderBed` 가 꼬리 1.5초를 앞에 감는다) 원인은 `<audio loop>` 의 브라우저 되감기 갭이었다.
`audio.js` 에 **`SeamlessLoop`** 추가 — 같은 소스를 엘리먼트 두 개로 돌리고 끝 1.2초에서
등파워(√)로 넘긴다. `loop=true` 는 교대를 놓쳤을 때의 안전망으로 남겼다.
시작 페이드도 400ms → 1500ms (400ms 는 "켜졌다"로 들린다).

**5. 그만두기밖에 없었다.** `일시정지` 버튼 추가(`#pause-btn`, 라벨이 "이어 듣기"로 바뀐다).
`NarrationPlayer.pause()/resume()` 을 새로 만들어 **침묵 중이면 남은 침묵부터, 말하는 중이면
그 파일 그 자리부터** 이어진다. 알람·취침 기록·`keepAlive` 는 안 건드린다 — 잠깐 멈춘 것이지
그만둔 게 아니다. 잠금화면 재생/일시정지 버튼도 여기로 연결했다
(예전엔 `narration.stop()` 이라 되돌릴 수 없었다).

**6. 배경음 3종 추가** — `synth.js` 에 생성기 3개(`genThunder`·`genCabin`·`genCave`),
`data/sounds.json` 에 레이어 3개. 사운드 10종 → **13종**, 여전히 음원 파일 0개.
- **먼 천둥**(60초) — 비 오는 밤의 깊이. 벼락 '쾅'은 일부러 뺐다, 놀라서 깨면 수면 앱이 아니다
- **기내 소음**(24초) — 선풍기와 달리 날개 통과음이 없고, 브라운 노이즈와 달리 동체 공명이 있다
- **동굴 물방울**(40초) — 방울마다 0.2초 뒤 작은 반향 하나. 그게 "동굴"을 만드는 전부다

**7. 배경 씬 3종 추가** — `scenes.js` 에 **눈 · 오로라 · 달과 구름**. 4종 → **7종**.
셋 다 사진·영상 없이 캔버스만 쓴다(파일 0바이트 → 루프 되감기 자체가 없다).
밤새 도는 그림이라 그리기 비용을 눌렀다 — 오로라는 커튼당 그라데이션 1개(세로선을 촘촘히
세우면 프레임마다 수백 개가 된다), 구름은 스프라이트를 미리 그려 두고 옆으로 밀기만 한다.

**8. 배경 영상이 되감을 때 번쩍였다.** 끝 프레임과 첫 프레임이 다르니 `<video loop>` 가
한 프레임 만에 튄다. 소리와 같은 방법 — `#scene-video` 를 `<div>` 로 바꾸고 안에 `<video>` 두 개를
넣어 끝 1.2초에서 선형으로 겹쳐 넘긴다(`SceneRenderer._videoTick`).
층 전체 밝기(`is-on`·`is-dim`)는 감싸는 div 가 갖는다 — 한 엘리먼트에 밝기와 교대를 같이 얹으면
서로 덮어쓴다.

**9. (검증 중 발견) 배포해도 화면이 안 바뀔 뻔했다.** `sw.js` 는 앱 파일을 "네트워크 우선"으로
가져온다고 적혀 있었지만, 그 `fetch` 가 브라우저 HTTP 캐시를 그대로 탔다 — 파일을 고쳐도
낡은 `scenes.js`·`sounds.json` 이 돌아왔다(실측으로 걸렸다). `cache: 'reload'` 를 붙여 고쳤다.
오프라인 폴백은 그대로다(`.catch` → 캐시).

**검증** — `node test.js` 29개 통과. `audio.js`·`synth.js`·`narration.js`·`sleep.js` node 로드 OK.
로컬 서버(8123)·모바일 뷰포트에서 실제로 돌리고 DOM·볼륨·영상 불투명도를 0.4~0.5초마다 샘플했다.
`=== 새로고침 이후 ===` 마커 뒤로 콘솔 에러 없음(그 앞의 에러 2개는 위 9번 캐시 불일치 때 난 것).

| 확인한 것 | 증거 |
|---|---|
| 홈에서 배경음 재생 | 제스처 후 1.5초 — `waves 0.126` `wind 0.0202` `paused:false`, play() 전부 resolve |
| 볼륨 계산 | 0.126 = 0.6²×0.35 ✓ · 덕킹 후 0.028 = 0.6²×0.35×0.22 ✓ |
| 시작 순서 | 0.5초 게이지 `opacity 0` · 4초 첫 자막("잠시 멈추겠습니다") · **41.5초 게이지 ON → 42.5초 "숨을 세어 보겠습니다"**(breath 층) |
| 이음매 없는 루프 | 3초 톤으로 9초 재생, 넘김 2회 — **무음 샘플 0개**, 합성 파워 0.0896~0.0903 (목표 0.09, 리플 0.03dB) |
| 실제 앱에서의 교대 | 41.5초 시점 로그에 옛 엘리먼트 2개 `paused`, 새 엘리먼트가 `0.028`/`0.004` 로 이어받음 |
| 일시정지 | 전 요소 `paused`, `keepAlive` 만 0.001 유지(알람 살아 있음), 라벨 "이어 듣기" · 아이콘 `#i-play` |
| 이어 듣기 | 배경음 0.028/0.004 복귀, 씬 비디오 재생, 자막이 다음 조각으로 진행 |
| 그만두기 | 홈 복귀 · 게이지 opacity 리셋 · 버튼 라벨/아이콘 원복 |
| 새 배경음 3종 | `render_beds.mjs` — thunder RMS 0.1031 / 피크 0.572, cabin 0.1090 / 0.416, cave 0.1091 / 0.409. 기존 가족(0.106~0.110)과 같은 크기, 헤드룸 0.58 안 |
| 새 배경음 브라우저 생성 | thunder 378ms · cave 262ms · cabin 168ms (기존 최대 280ms → thunder 가 60초라 가장 느리다. 슬라이더를 처음 올릴 때 한 번뿐) |
| 씬 7종 전부 그려짐 | 오프스크린 캔버스로 렌더 후 픽셀 집계 — 눈 25.4% `rgb(186,206,236)` · 오로라 34.9% `rgb(143,186,198)` · 달과 구름 77.7% `rgb(161,173,201)` |
| 영상 되감기 번쩍임 | waves.mp4(16.0초) 실측 — 14.8초까지 A 단독 `1.00`, 15.2초 `A .74 / B .26`, 15.6초 `A .40 / B .60`, 이후 B 단독. **합이 항상 1.00** → 번쩍임도 어두워짐도 없다 |
| 목록 반영 | 믹서 13줄(thunder·cave·cabin 포함) · 씬 칩 7개 |

### 2026-08-21 (5) — 배포 (0a99627)

- (2)(3)(4) 전부 — 배경 사진·비디오, 호흡 게이지 명상화, 숫자 세기 정밀화 — 를
  사용자 승인 후 push. 실서비스 확인은 Pages 빌드 후

### 2026-08-21 (4) — 배경을 루프 비디오로 (배포됨 → 0a99627)

- **씬 4종에 무음 루프 비디오** (`assets/scenes/*.mp4`, Pexels License, 540×960 재인코딩,
  총 6.8MB): 은하수 타임랩스 7초 / 유리창 빗물 20초 / 모닥불 클로즈업 18초 /
  달빛 반짝이는 밤바다 16초. 프레임 추출로 전부 눈 검수. 출처는 assets/LICENSES.md
- **배경 3층 재구성**: 바닥(#scene-back: 사진+그라데이션) → 비디오 → 캔버스(스크림+입자).
  비디오는 첫 프레임이 실제로 나올 때만(playing 이벤트) 1.5초 페이드인 —
  로딩·실패·자동재생거부 어느 경우든 사진 배경이 받친다
- **배터리 규칙**: 비디오는 세션 중에만 재생. 딤(나레이션 끝)·화면 숨김이면 정지
  (MutationObserver 로 is-dim 감시, 앱 코드 무수정). 정지 프레임은 opacity 0.3 으로 남는다
- sw.js 캐시 우선 대상에 .mp4 추가
- 검증: 세션 시작 → waves.mp4 선택·로드(readyState 4)·재생(0.81초 진행)·is-on 페이드인,
  세션 종료 → 정지·페이드아웃, 콘솔 오류 0건. (숨긴 패널에선 document.hidden 가드가
  재생을 막는 게 정상 동작이라 hidden 스텁으로 검증)
- **배포 안 함** — 사용자 승인 대기

### 2026-08-21 (3) — 호흡 게이지를 명상답게 (미배포)

- "너무 빠르고 정신없다"의 원인은 속도가 아니라 **자극 밀도**: 위상마다 링 리셋 +
  라벨 교체 + 숫자 1 복귀가 동시에 터져 12초에 세 번 화면이 "새로 시작"했다
- 링을 위상별 리셋 → **사이클(12초)당 한 바퀴 연속 진행**으로. 위상 경계에서 링은
  아무 일도 안 한다 (수학적으로 이어지는 지점에 무전환 재설정)
- 라벨은 260ms 크로스페이드, 멈추기(2초)는 숫자 대신 점(·) 하나
- 숫자 34px/흰색 → 24px/보조색 — 안내지 주인공이 아니다. 원판 팽창 0.74→0.8 (잔잔하게)
- 검증: 브라우저에서 링 transition "stroke-dashoffset 12s linear" · hold 에 '·' 표시 ·
  콘솔 오류 0건
- **배포 안 함** — 사용자 승인 대기

### 2026-08-21 (2) — 배경 실사진 + 호흡 박자 정밀화 (미배포)

- **재생 배경 = 실사진 + 캔버스 애니메이션 합성**: `assets/scenes/*.jpg` 4장
  (은하수·유리의 빗방울·달빛 바다·모닥불, 세로 1080×1700, 총 1.3MB, 전부 눈으로 검수).
  사진 위에 어둠 스크림, 그 위에 별 반짝임·유성·빗줄기·파문·물결 띠·불티·연기가 움직인다.
  사진과 겹치던 합성 요소(가짜 은하수·가짜 달·달빛 반짝임)는 제거.
  사진을 못 받으면 기존 그라데이션이 그대로 받친다 (CSS 다층 background)
- **호흡 타이머 정밀화** — "타이머가 빠르다"의 진짜 원인: 숫자 파일이 0.7초인데
  고정 침묵 0.5초를 더해 음성이 1.2초 주기 → 1.0초짜리 게이지가 목소리보다 앞서갔다.
  수정: 침묵 = 1.0초 − 방금 파일의 실측 길이 (`_lastDur`), 게이지 초읽기는 벽시계 기준.
  preview.py 도 같은 규칙 (ffmpeg 로 파일 길이 실측)
- 검증: `node test.js` 29개 통과 · scene background 3층(스크림/사진/그라데이션) 합성 확인,
  assets/scenes 200 · 세션 시작 → 사진 배경 위 재생 화면, 콘솔 오류 0건
- **배포 안 함** — 사용자 승인 대기

### 2026-08-21
- **나레이션 문장에 한글 명조 적용** (`index.html`, `style.css`). brands-design 스킬의 warp 레퍼런스에서
  "산세리프 일색에 세리프를 한 군데만" 아이디어를 가져왔다. 바꾼 곳은 `.caption` 하나뿐.
  - warp가 쓰는 **Instrument Serif 는 라틴 전용이라 못 썼다.** 나레이션 81조각은 라틴 0자·한글 7,211자
    (고유 419자)라 글리프가 하나도 안 나온다. 한글 명조로 대체.
  - **Gowun Batang** 선택. 명조 중 획 대비가 낮아 어두운 배경에서 가늘어지지 않는다.
    Nanum Myeongjo 는 hairline 이 18~19px 다크에서 부서져 보인다.
  - **이탤릭은 쓰지 않았다.** 한글은 이탤릭 전통이 없어 브라우저 합성 기울기가 획을 무너뜨린다.
  - 비용: 구글 서브셋 95조각 중 나레이션이 건드리는 **27조각 = 총 461KB**, 재생 화면에서만
    필요한 조각씩 나눠 받고 캐시된다. 첫 세션 실측 10조각 182KB.
    무겁다고 판단되면 419자 self-host woff2(~50KB)가 업그레이드 경로 (`index.html` 주석에 기록).
  - 검증: `node test.js` 29개 통과 / 재생 화면에서 `Gowun Batang` 27개 중 10개 로드 확인,
    computed `font-family` 적용 확인, 모바일 뷰포트 렌더 확인.
  - **배포됨** (7ba1b37, main+gh-pages).

### 2026-08-19 (6) — 프리셋 카드를 실제 사진으로 + 피드백 배포

- (5)의 피드백 8건을 f20f0ef 로 배포
- **프리셋 카드 10장 전부 Unsplash 사진으로 교체** (`assets/presets/*.jpg`, 총 ~330KB,
  카드 크기 440×280 크롭). 전부 눈으로 검수 — 달빛 해변·빗방울 보케·모닥불 불티·
  반딧불 궤적·이끼 계곡물·파스텔 새벽 수평선·빈티지 선풍기·눈 오는 가로등 길·램프 켠 방.
  출처·사진가는 `assets/LICENSES.md` (Unsplash License — 상업 무료·표기 불요)
- 사진 로드 실패(오프라인 첫 방문) 시 기존 SVG 일러스트가 자동으로 받친다 (`onerror` 제거)
- 사진 위 saturate 0.92·brightness 0.88 + 상단 비네트 — 톤이 제각각인 사진을 한 세트로
- `og:image` 추가 (밤바다) — 링크 공유 시 미리보기
- 검증: assets 200 응답·440×280 디코드 확인, 카드 10장 img+SVG 폴백 구조 렌더, 콘솔 오류 0건
  (lazy 이미지의 실제 표시는 실기기에서 확인 — 헤드리스 패널은 lazy 를 트리거하지 않음)

### 2026-08-19 (5) — 실사용 피드백 8건 반영 (배포됨 → f20f0ef)

- **배경음 추가 감량**: 제곱 커브 위에 마스터 0.55 (`Mixer._target`). 파도 60% 슬라이더 → 실제 12%
- **나레이션 숫자 세기를 실제 초처럼**: "하나, 둘, 셋, 넷"을 숫자별 토막으로 쪼개고
  (chunking.py·audio.js 동기 규칙, COUNT_GAP 0.5초 고정) 영향받은 조각 재생성.
  프리뷰에도 같은 박자 반영
- **호흡 게이지 ↔ 나레이션 동기화**: 나레이션이 "하나"를 말하는 순간 직전 동사(들이쉬/내쉬)를
  보고 게이지를 그 위상으로 되감는다 (`narration.onChunk`). 위상 전환 시 카운터 인터벌 정리
- **클로징 멘트 보강**: 밤 상태 4종 + awoken 의 fade 를 5문장 → 7~8문장으로.
  "지금 잠들지 않아도 괜찮습니다 · 소리는 밤새 곁에 있습니다" 계열 — 여전히 인사 없이 소실
- **루프 이음매 제거**: 이벤트(빗방울·장작·처프·차임)를 원형 버퍼로 분리해 꼬리가 루프
  시작으로 감기게, 파도 포락선도 순환, 텍스처 크로스페이드 0.5→1.5초.
  실측: 이음매 전후 0.3초 에너지비 1.04~1.12배 (수정 전엔 포락선이 뚝 끊겼음)
- **풀벌레·장작 실제 녹음**: archive.org 퍼블릭 도메인(radio aporee) —
  풀벌레 = Jillis Molenaar, Blumlein 스테레오 야간 녹음 65초 발췌 /
  장작 = Piotrek Zyla, 벽난로 76초 발췌. `audio/loops/*.mp3` (2.7MB), 출처는 audio/LICENSES.md.
  재생은 디코드→loopify(이음매 재조립+정규화)→WAV Blob — mp3 루프 틈 원천 차단.
  파일을 못 받으면 합성이 자동으로 받친다 (sounds.json 에 file+generate 병기)
- **배경 씬 업그레이드**: 별하늘=은하수(오프스크린 1회 렌더)+별색 변주+18~44초마다 유성 /
  창밖 비=원근 2겹+바닥 파문+물안개 / 파도=수평선 달+물에 비친 달빛 반짝임+물마루 하이라이트 /
  모닥불=연기 3가닥+상승기류에 흔들리는 불티+가끔 세게 튀는 불티
- 검증: `node test.js` 29개 통과 · 매니페스트 788개 참조/누락 0 · 브라우저에서 fire.mp3
  fetch 200 → 12.5MB WAV 블롭 생성·재생, 콘솔 오류 0건 · 미리듣기 racing 5:50, anxious 5:29
- **배포 안 함** — 사용자 승인 대기

### 2026-08-19 (4) — 배포

- 사용자 승인 후 `git push origin main gh-pages` (ec8f173 → acf675e).
  8/14 이후 쌓인 전부: 나레이션 710개 mp3 + synth.js v2 + 알람 수정 + 프리셋 SVG +
  호흡 게이지 + 홈 히어로 + sw.js + tools/ 생성 스크립트
- `tools/__pycache__` 는 커밋에서 빼고 `.gitignore` 에 추가
- 실서비스 확인은 Pages 빌드 후 (아래 로그의 미배포 표기는 이 시점으로 해소)

### 2026-08-19 (3) — 홈 화면 히어로 (미배포)

- 홈에 히어로 블록: **큰 시계(56px) + 날짜·요일·시간대**("8월 19일 수요일 · 깊은 밤") +
  **오늘 달** — 실제 위상을 계산해(삭망월 29.53일 근사) 그림자 원 위치로 그믐~보름을 그린다.
  달은 7초 주기로 살짝 떠다닌다 (reduced-motion 시 정지)
- 상태 카드 점을 상태별 고유색으로 (racing 보라 · anxious 청록 · wired 호박 ·
  awoken 남보라 · unknown 중성) — 다섯 줄이 한 덩어리로 보이던 문제
- 홈의 제목·부제 중앙 정렬, 헤더의 작은 시계는 히어로로 승격되며 제거
- 검증: 브라우저에서 시계 00:18 · 날짜줄 · 달 그림자 cx=-15.5 · 점 5색 렌더 확인, 콘솔 오류 0건
- **배포 안 함** — 사용자 승인 대기

### 2026-08-19 (2) — 호흡 게이지 + 디자인 다듬기 (미배포)

- **호흡 게이지 신설** (재생 화면): SVG 진행 링(12시부터 선형으로 차오름, 멈춤 단계는 등불색)
  + 들숨에 부풀고 날숨에 잦아드는 원판 + 초 단위 숫자 카운트업.
  4-2-6 박자 — 나레이션 breath 층("넷을 세며 들이쉽니다")과 같은 방향·같은 박자.
  기존 CSS 키프레임(12초 고정 루프)은 JS 위상과 어긋나므로 제거, 전부 JS 가 클래스로 지휘
- 디자인 스킬 기준(make-interfaces-feel-better · 12-principles) 점검 결과 반영:
  화면 진입 420→280ms / 스태거 간격 최대 90→35ms (50ms 규칙) /
  시작 버튼을 그라데이션+글로우로 (앱의 유일한 밝은 면) / 알람 시계에 등불 글로우
- reduced-motion: 원판은 멈추되 숫자 카운트·링은 그대로 안내
- 검증: `node test.js` 29개 통과 · 콘솔 오류 0건 · 세션 시작 후 위상 클래스 `is-in`,
  카운트 2.2초에 "2", stroke-dashoffset CSSTransition `running` 확인
  (패널이 숨겨져 시각 렌더만 정지 — 실기기 확인 필요)
- **배포 안 함** — 사용자 승인 대기

### 2026-08-19 — 알람 버그 수정 · fire 재설계 · 프리셋 SVG · 볼륨 커브 (미배포)

- **알람이 안 울리던 원인 둘을 특정해 수정**:
  ① `fade()` 가 틱 횟수로 진행을 셌다 — 백그라운드 탭은 setInterval 이 1초로 스로틀되어
  60초 램프가 실제로는 20분이 됐다. 벽시계(Date.now) 기반으로 교체.
  ② 알람 시작 볼륨이 0.02 라 첫 1분이 사실상 무음이었다 → 0.15 에서 시작해 램프.
- **fire 재설계**: 감쇠 사인 탁탁이 전자음 '삐' 로 들린다는 피드백 →
  광대역 노이즈 클릭(`panNoise`) + 약한 나무 울림 + 가끔 두 번 갈라지는 쩍- 으로 교체.
  옹이·불티도 노이즈 기반으로.
- **프리셋 카드에 장면 일러스트**: 사진은 라이선스가 걸리므로 인라인 SVG 8종
  (달과 파도·유리창의 비·장작불·반딧불 마당·계곡·선풍기·눈·새벽, `app.js` ART).
  기존 CSS 텍스처는 SVG 와 겹쳐서 제거.
- **배경음 기본 크기 줄임**: 슬라이더값을 제곱 커브로 매핑 (`Mixer._target`).
  선형이면 슬라이더 초입부터 너무 크다 — 귀는 로그로 듣는다. 프리셋 기본값도 같이 조용해짐.
- 검증: `node test.js` 29개 통과 · 전 사운드 RMS 편차 1.06배, 피크 한도 내 ·
  브라우저에서 프리셋 카드 10장 전부 SVG 렌더 확인 ·
  **알람 실전 테스트 통과** — 00:07 예약 → 00:07 정각 알람 화면 발화 → 끄기 → 밤 기록 생성,
  콘솔 오류 0건. (소리 자체는 자동화 클릭이 신뢰 제스처가 아니라 브라우저 정책상 확인 불가 —
  실기기에서 한 번 확인 필요)
- **배포 안 함** — 사용자 승인 대기

### 2026-08-18 — 사운드 합성 v2: 스테레오 44.1kHz 전면 재작성 (미배포)

- 사용자 피드백 "TTS 외 모든 소리 퀄리티가 무척 안 좋다" → 원인 셋을 특정:
  ① 모노(이어폰에서 머리 안에 갇힘) ② 22050Hz(11kHz 위가 없어 비·시냇물의 공기가 죽음)
  ③ 이벤트가 전부 백색노이즈 버스트(물방울은 '탁'이 아니라 '통' — 공명이 필요)
- DSP 를 `synth.js` 순수 모듈로 분리 (node 렌더 가능). `audio.js` 는 Blob 포장만
- 전 사운드 재설계: 비=히스+중역워시+틱/드립 2종 물방울 / 파도=부서짐(고역)·물러남(저역)
  분리 포락선, 파도마다 좌우 위치 이동 / 모닥불=화염 웅웅+공명 탁탁+옹이+큰 장작(쿵+불티) /
  시냇물=공명체 5대역 워블 / 풀벌레=밤공기 베드+5마리(피치 슬라이드·세기 편차·쉼·원근) /
  바람=돌풍 공유 포락선+창틈 휘파람 / 선풍기=험 2배음+날개 AM / 노이즈 3종=채널 비상관 스테레오
- 알람을 핑크노이즈 → 3음 차임(C5·E5·G5, 9초 루프)으로 교체. 60초 램프는 유지
- 진짜 녹음(CC0) 검토 후 보류: mp3 `<audio loop>` 는 인코더 패딩 때문에 루프마다 틈이 생기고
  (조용한 방에서 제일 거슬리는 그것), 품질을 귀로 못 고른 채 내려받는 도박이라
  합성 v2 를 먼저 검수받기로. 부족하면 CC0 수급 + 디코드→WAV 재조립 경로가 다음 단계
- 검증: `node tools/render_beds.mjs` — 11종 RMS 편차 1.06배, 피크 전부 한도(0.58) 내,
  생성 최대 280ms. 브라우저에서 세션 시작 → 파도 7.4MB·바람 6.1MB 스테레오 블롭 생성·재생,
  콘솔 오류 0건. `audio/_samples/사운드-*.mp3` 미리듣기 11종 렌더
- **배포 안 함** — 사용자 승인 대기

### 2026-08-14 (2) — 나레이션 전 상태 완성 + 오프라인 지원 (미배포)

- **6개 상태를 5층 구조로 재작성** (`tools/rewrite_states.py`): anxious·wired·unknown
  각 15조각(조합 243), awoken은 settle+fade만 6조각(밤중에 다시 깨우지 않기),
  calm은 settle+breath+fade 9조각(깨어난 채 끝남), nap은 settle+release 6조각(심상 없음).
  상태별 강조는 층 분량으로(anxious=호흡, wired=이완). 어휘 소유 감사 통과
  (가라앉다→drift, 멀어지다→fade, 숨→breath, 어깨·무거워지다→release).
  심상도 상태마다 분리: racing 파도·계단·비 / anxious 눈·호수·강 /
  wired 모닥불·마을 소등·저무는 하늘 / unknown 밤 기차·달·갈대밭
- **확정 설정으로 TTS 558개 생성** (Algieba·0.69·EQ). 429 쿼터로 49개 실패 → 재실행으로 채움.
  옛 Edge 음성 파일은 삭제. 최종 710개·9.1MB, 매니페스트 `files` 참조 710개 전부 존재 확인
- calm 대본의 "삼 분" 언급 제거(실측 2분과 어긋남) 후 calm만 재생성
- **서비스 워커 추가** (`sw.js`): 앱 파일은 네트워크 우선+캐시 폴백, mp3는 캐시 우선.
  브라우저 확인 — SW activated, 셸 11개 프리캐시, 재생한 mp3가 캐시에 쌓임
- 설정 화면 정리: "목소리" → "나레이션". 침묵 조절을 앞으로, 브라우저 TTS(음성·속도·톤)는
  "기기 내장 음성 (예비)" 접이식으로 강등 — 이제 전 상태가 파일 음성이라 예비일 때만 쓰인다
- 검수: `node test.js` 29개 통과 · 콘솔 오류 0건 · anxious 재생 시 `s1-0.mp3`~ 200 응답 확인
- 미리듣기 6개 렌더 (`audio/_samples/`): anxious 5:16 · wired 4:57 · unknown 4:45 ·
  awoken 1:03 · calm 2:02 · nap 1:28
- **배포 안 함** — 사용자 승인 대기

### 2026-08-14 — 방문자 측정 · 검색 노출 기본 세팅

- `index.html` `<head>`: `description` 을 실제 기능(나레이션·자연음 믹서·타이머/알람·기록)으로 보강,
  Open Graph 4종(`og:title`·`og:description`·`og:type`·`og:url`), `canonical` 추가.
  이미 있던 `theme-color`·`manifest` 는 건드리지 않음
- GA4 스니펫 삽입. **측정 ID가 `G-XXXXXXXXXX` 인 동안은 스스로 꺼진다** — 스크립트를 만들지도,
  요청을 보내지도 않는다. 브라우저에서 확인: `gtag` undefined, googletagmanager 요청 0건
- 서치콘솔 인증 `<meta>` 를 붙일 자리를 안내 주석으로 표시
- `robots.txt` · `sitemap.xml` 신규. 배포되는 HTML 은 `index.html` 하나뿐이라 사이트맵 URL 도 루트 하나
- `.gitignore` 는 손댈 필요 없었음 — `git check-ignore` 로 두 파일 모두 제외 대상이 아님을 확인
- 검증: `node test.js` 29개 통과, 로컬 서버에서 페이지 로드 시 콘솔 오류 0건, 홈 화면 정상 렌더

### 2026-08-12 (2) — 사운드 합성 전환 + 배포

- 음원 다운로드 시도 → Mixkit 라이선스가 "소스 파일과 함께 재배포"를 금지해 공개 저장소에 못 올림.
  위키미디어 CC0 는 파일이 20~90MB. 합성으로 전환
- 사운드 7종 신규 합성 (비·파도·모닥불·바람·시냇물·풀벌레·선풍기). 주기 성분을 루프 길이의
  정수 배로 맞춰 이음매 제거. 볼륨 처음 올릴 때만 생성(10~53ms)
- **측정으로 잡은 문제 둘**:
  백색소음 피크 1.18 → 22050Hz 리샘플링 보간 오버슈트로 클리핑. 여유를 0.58 로
  피크 정규화가 무의미했음 — RMS 가 모닥불 0.051 vs 백색 0.385 로 같은 슬라이더에서 7배 차이.
  RMS 기준 정규화 + tanh 소프트 리미터로 편차 7.5배 → **1.08배**
- git 저장소 생성, GitHub Pages 배포. `gh-pages` 브랜치 푸시로 Pages 자동 활성화됨

### 2026-08-12 (1) — 기획 + 프로토타입 구현

- Calm / Renight 실사용 불만을 정리해 문제를 "반복"으로 특정. 설계문서 작성
- 리서치: 일반 TTS 모델은 긴 침묵을 오류로 보고 줄인다 → 실시간 생성 불가, 미리 생성이 맞음을 확인
- 이름 `굿슬립` 확정. 영문 GoodSleep은 동종 앱이 이미 여럿이고 서술적이라
  상표로 소유하기 어려움 → `APP_NAME` 상수 한 곳으로 분리해 교체 비용을 0으로 만듦
- 전체 구현 (11개 파일). 정적 파일 + ES 모듈, 빌드 없음
- **테스트가 잡은 버그**: 중복 배제 폭을 `pool-1`로 두니 후보가 하나만 남아
  선택이 결정론적 순환이 됨 — 조합 36개가 12개만 돌았다. 1/3로 줄여 해결
- **검증 중 발견**: 같은 날짜 밤 기록이 둘이면 부채는 둘 다 더하는데 차트는 하나만 그림.
  `upsertNight`로 날짜당 하나 보장
- 대비 검사에서 3차 텍스트 4.47 (AA 미달) → 토큰 조정해 5.20
