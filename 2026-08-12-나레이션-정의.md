# 숙면 명상 나레이션 정의

> 작성: 2026-08-12 · **갱신 2026-09-04** — 7층 구조와 실측 반영
> 대상: `data/narration.json` 의 모든 대본
> 실측 근거: [시나리오 분석](2026-09-02-나레이션-시나리오-분석.md)
> 상위 문서: [설계문서](2026-08-12-굿슬립-설계문서.md)

---

## 1. 왜 이 문서가 필요한가

지금 대본은 문장 자체는 나쁘지 않지만 **구조가 없다.**
도입 1 + 본문 1 + 마무리 1 = 조각 3개, 말하는 시간 60~90초.

그 시간은 채우는 게 아니라 **단계를 밟는 데** 쓴다.
단계 없이 좋은 문장만 나열하면 "상담"이 되고, 단계가 있으면 "유도"가 된다.

**목표 길이는 15~20분이다.** 이 문서는 처음에 5~7분으로 잡았는데,
2026-09-02 실측에서 그게 틀린 것으로 드러났다 — 잠드는 데 10~20분이 걸리므로
7분에 끝나면 정작 잠드는 구간 전체가 무음이다. 근거는 분석 문서 2절.

---

## 2. 핵심 원리: 능동 → 수동

수면 명상의 뼈대는 이것 하나다.

```
바쁜 머리를 붙잡을 만큼 능동적인 과제로 시작해서,
점점 아무것도 하지 않는 상태로 넘긴다.
```

처음부터 "아무 생각도 하지 마세요"라고 하면 실패한다. 머리가 아직 그럴 상태가 아니다.
그래서 **셀 것을 주고 → 느낄 것을 주고 → 볼 것을 주고 → 놓아준다.**

여기서 "셀 것"은 **말로 하는 호흡 지시**다. 대본에 `하나·둘·셋` 같은 **숫자 세기를
넣지 않는다** — 2026-08-26 에 전량 제거했다. 한두 음절 토막은 TTS가 음절당 0.277초로
늘여 읽고 음량도 최대 5배 작게 내서, 단어가 아니라 앓는 소리로 들렸다.
숫자를 세게 하려면 홈의 별도 호흡 화면(`s-breathe`)이 맡는다.

작동 경로는 셋이다.
1. **부교감 활성화** — 날숨을 들숨보다 길게 하면 몸이 안전하다고 판단한다
2. **인지적 이탈** — 걱정에서 주의를 떼어 중립적 대상(숨·몸·심상)으로 옮긴다
3. **각성 하강** — 호흡과 심박을 수면 개시 생리에 맞춘다

---

## 3. 7층 구조

기존 3층(intro/body/outro)을 5층으로 바꿨고(2026-08), 2026-09-02 에
**이완을 하체·상체로 가르고 여운을 더해 7층**이 됐다.

아래 말·침묵은 밤 4개 상태(racing/anxious/wired/unknown)의 **ffprobe 실측 평균**이다.
`배수` 는 `audio.js` 의 `LAYER_GAP` — `sentenceGap`(5.0초)에 곱해 문장 사이 침묵이 된다.
`음량` 은 `audio.js` 의 `LAYER_VOL` — 사용자 음량에 곱한다.

| 층 | 이름 | 역할 | 말 | 침묵 | 배수 | 음량 | 문장 |
|---|---|---|---|---|---|---|---|
| `settle` | 정착 | 자세, 지금 여기, 그만해도 된다는 허락 | 26초 | 51초 | 1.2 | 1.00 | 보통 |
| `breath` | 호흡 | 날숨 늘이기. 말로만 이끈다 | 16초 | 50초 | 1.8 | 0.98 | 보통 |
| `release` | 이완(하체) | 발끝→골반. 부위당 오래 머문다 | 21초 | 146초 | 3.6 | 0.94 | 짧게 |
| `release2` | 이완(상체) | 배→이마. 같은 동사군으로 이어 올라온다 | 21초 | 159초 | 3.6 | 0.90 | 짧게 |
| `drift` | 심상 | 수동적 이미지. 이제 시키지 않는다 | 21초 | 128초 | 3.0 | 0.85 | 아주 짧게 |
| `fade` | 소실 | 마무리하지 않는다. 그냥 사라진다 | 12초 | 81초 | 3.2 | 0.78 | 단문 |
| `tail` | 여운 | 말이 뜸해지다 사라진다 | 12초 | 108초 | 4.6 | 0.70 | 단문 |

층 사이에는 **후렴**(`anchor`)이 한 문장씩 들어간다 — 밤 세션에 6번. 2~3초짜리
한 조각이고, 음량은 앞 층을 물려받는다 (4.3.2).

합계 말하는 시간 약 2분. 조각 사이 침묵(6초)까지 넣으면 **평균 15.5분**
(상태별 실측 범위: racing 14:37~16:59 · anxious 15:14~16:47 ·
wired 13:34~16:38 · unknown 14:48~16:10 · `awoken` 1:18 · `calm` 3:01~3:48 ·
`nap` 3:18~3:36).
**말 14 : 침묵 86** — 이 비율은 수면 나레이션으로 알맞아서 손대지 않는다.

### 느려지는 감각은 두 축으로 만든다

침묵(`LAYER_GAP` 1.2 → 4.6)만으로 만들던 것을 2026-09-04 에 **음량**(`LAYER_VOL`
1.00 → 0.70, 전체 -3.1dB)으로 한 축 더 만들었다. 명상 녹음은 실제로 뒤로 갈수록
작아진다. **폭은 좁게 유지한다** — 크게 내리면 `tail` 이 안 들리는데, 여운은
안 들리는 게 아니라 들리다 사라져야 한다. mp3 는 손대지 않고 재생 시점에 곱한다.

> `awoken`(자다가 깼어요)만 예외로 `settle` + `fade` 2층 · **90초**를 지킨다.
> 다시 깨우면 안 되기 때문이고, 매니페스트의 `gapScale: 0.7` 로 침묵을 줄여 맞춘다.

### 침묵이 뒤로 갈수록 길어지는 이유

이게 "점점 느려진다"는 감각을 만드는 진짜 장치다.
말의 속도를 늦추면 음질이 망가지지만(타임스트레치), 침묵을 늘리면 아무것도 망가지지 않는다.
`settle` 배수 1.2 에서 `tail` 4.6 까지 약 4배로 벌어지는 동안 듣는 사람은
자기가 느려지고 있다고 느낀다. 실제 문장 사이 침묵은 `settle` 평균 4.6초 →
`release` 14.3초 → `tail` 18.9초다 (문장이 짧을수록 `sentenceScale` 이 다시 줄인다).

### 수면 명상에는 마무리가 없다

일반 명상은 "이제 천천히 눈을 뜨고 돌아옵니다"로 끝난다. **수면 명상은 그러면 안 된다.**
`fade` 와 `tail` 은 결론을 내지 않는다. 정리하지 않는다. 인사하지 않는다.
문장이 점점 짧아지다가 그냥 없어진다. 듣는 사람이 언제 끝났는지 모르는 게 성공이다.

`tail`(여운)은 2026-09-02 에 "끝이 절벽이다"라는 진단을 받고 생긴 층이다.
`fade` 로 끝내면 마지막 말 뒤에 아무 예고 없이 정적이 오는데, 그 낙차 자체가
사람을 깨웠다. `tail` 은 문장 사이를 ~19초까지 벌려 말이 뜸해지다 사라지게 한다.

---

## 4. 문장 규칙

### 4.1 어미가 목소리를 정한다 — 가장 중요

TTS는 **문장의 어미를 보고 억양 곡선을 정한다.**

| 어미 | 억양 | 신호 | 수면에 |
|---|---|---|---|
| `~해 보세요` | 끝이 올라감 | 권유 — "당신 차례입니다" | ✗ 응답을 요구한다 |
| `~하세요` | 평평하거나 올라감 | 지시 | ✗ 과제를 준다 |
| `~합니다` / `~됩니다` | **내려감** | 서술 — 끝났다 | ✓ |
| `~있습니다` / `~괜찮습니다` | **내려감** | 확인 | ✓ |

지금 대본은 `~해 보세요`가 지배적이다. **그래서 상담사처럼 들린다.**
목소리 문제가 아니라 문법 문제다. 어미를 서술형으로 바꾸면 같은 엔진에서도 억양이 내려간다.

```
전: 숨을 천천히 내쉬어 보세요.        ↗ 권유
후: 숨이 천천히 빠져나갑니다.          ↘ 서술
```

**권유형은 `settle`·`breath` 에서만 쓴다.** 아직 능동 구간이기 때문이다.
`release` 이후로는 서술형만 쓴다.

### 4.2 층마다 어휘를 나눈다

한 세션 안에서 같은 표현이 두 번 나오면 즉시 티가 난다.
층이 다르면 다른 층인 줄 모르고 "아까 그 말 또 하네"로 들린다.

**층마다 소유 어휘를 정하고 남의 것을 쓰지 않는다.**

| 층 | 이 층의 말 | 쓰지 않는 말 |
|---|---|---|
| `settle` | 자리, 자세, 받치다, 끝나다, 내려놓다 | 가라앉다, 멀어지다, 무거워지다 |
| `breath` | 숨, 세다, 들이쉬다, 내쉬다, 배, 맡기다 | 가라앉다, 무거워지다, 몸 부위 |
| `release` | **하체 부위**(발·발목·종아리·무릎·허벅지·골반), 풀리다, 빠지다, 느슨해지다, 무거워지다, 펴지다 | 가라앉다, 멀어지다, 숨 |
| `release2` | **상체 부위**(배·가슴·등·손·팔·어깨·목·턱·눈·이마), `release` 와 같은 동사군 | 가라앉다, 멀어지다, 숨 |
| `drift` | 심상(파도·계단·비), 가라앉다, 내려가다, 줄어들다, 어두워지다 | 몸 부위, 숨, 멀어지다 |
| `fade` | 남다, 놓다, 멀어지다, 조용해지다 | 새 심상, 몸 부위, 숨 |
| `tail` | 두다, 지나가다, 잊히다, "할 일이 없다" | **소유 어휘 전부** — 새 말을 하지 않는다 |

`가라앉다` 는 `drift` 만, `멀어지다` 는 `fade` 만 쓴다.
겹치면 세션 하나에서 두 번 들린다.

이완이 두 층으로 갈린 뒤 소유는 **집합**이 됐다 — `무거워지다`·`어깨` 는
`release` 와 `release2` 가 함께 쓴다. 부위만 다르고 동사군은 이어져야 하기 때문이다.
검사기는 `tools/rewrite_states.py` 의 `OWNED` 이고, 대본을 고칠 때마다 돌린다.

### 4.3 `settle` 은 무엇을 할지 먼저 알려 준다

첫 층은 바로 지시로 들어가지 않는다.
**앞으로 몇 분 동안 무슨 일이 일어나는지 한 문장으로 알려 주고 시작한다.**
모르는 채로 지시를 받으면 "지금 뭘 해야 하지"라는 인지 부하가 생긴다 — 그게 각성이다.

```
이제 잠들 준비를 하겠습니다.
잠시 몸을 편하게 하고, 호흡을 고른 다음, 천천히 힘을 내려놓을 겁니다.
어려운 것은 없습니다. 그냥 듣고 있으면 됩니다.
```

**이 안내는 `settle` 에만 둔다.** 뒤에서 다시 설명하면 그때는 방해가 된다.

### 4.3.1 뒷층은 자기소개를 하지 않는다

2026-09-04 에 `breath` 이후 층의 첫 문장에서 "이제 ~하겠습니다" 류를 걷어냈다.
조각이 서로 교체 가능해야 하니 조각마다 스스로를 소개하고 있었는데,
7층이 다 그러면 15분 동안 자기소개가 일곱 번 나온다 — 한 세션이 아니라
클립 일곱 개를 이어붙인 것으로 들린다.

```
전: 이제 위쪽으로 올라오겠습니다. 배가 부드러워집니다. …
후: 배가 부드러워집니다. …
```

**뒷층은 하던 일을 이어서 한다.** 무엇을 할지는 `settle` 이 이미 예고했다.

예외는 **기법을 가르치는 첫 문장**이다 — wired 의 PMR(`pl1`·`pl2`·`pu1`)과
racing 의 인지 섞기(`ds1`~`ds3`)는 무엇을 하라는 건지 그 조각 안에서 알려 줘야 한다.
어느 조각이 뽑힐지 모르므로 다른 조각의 설명에 기댈 수 없다.
`calm` 의 `fade` 도 남긴다 — 깨어서 끝나야 하니 돌아오라고 말해야 한다.

### 4.3.2 후렴 — 반복을 설계로 쓴다

자기소개를 걷어내도 층 사이는 여전히 비어 있다. 조각이 서로 교체 가능해야 하니
**층끼리 이어지는 말을 가질 수 없기** 때문이다. 그 자리를 채우는 것이 후렴이다.

**상태마다 짧은 한 문장을 정해 층이 바뀔 때마다 끼운다** (밤 세션에 6번).
매니페스트의 `state.anchor` 에 조각 하나로 두고, `narration.js` 의 `buildSequence`
가 층 앞에 넣는다. 뽑기(`pickWithHistory`)를 거치지 않으므로 **매번 같은 문장**이다.

| 상태 | 후렴 |
|---|---|
| 생각이 안 멈춰요 | 붙잡지 않아도 됩니다. |
| 마음이 불안해요 | 여기서는 괜찮습니다. |
| 몸은 피곤한데 못 자요 | 몸은 쉬어도 됩니다. |
| 그냥 잠이 안 와요 | 여기 있으면 됩니다. |
| 잠깐 마음 가라앉히기 | 잠깐이면 됩니다. |
| 낮잠 | 짧게 쉬면 됩니다. |

`awoken`(자다가 깼어요)에는 두지 않는다 — 90초 상한을 지켜야 한다.

**왜 같은 말을 여섯 번 하는가.** 지금까지의 반복은 사고였다 — 층당 조각이 3개뿐이라
같은 조각이 3밤마다 돌아왔고, 그건 결함으로 들린다. 명상 전통은 반대로 간다.
**한 구절을 일부러 반복해서 그것이 세션의 척추가 되게** 한다 (4.4 "반복은 결함이 아니다").
우연한 반복은 결함이고 설계된 반복은 안정감이다.

후렴을 고를 때 지키는 것:
- **소유 어휘를 쓰지 않는다** — 어느 층에도 속하지 않아야 여섯 번 다 어울린다
- **코퍼스에 없는 문장으로 짓는다** — 이미 있는 문장이면 후렴인지 그 층의 말인지 흐려진다
- **서술형·10자 안팎.** 지시가 아니라 허락이다
- **새 정보를 넣지 않는다** (4.4)

### 4.4 그 밖의 규칙

- **의문문 금지.** 질문은 인지를 깨운다. `?` 를 쓰지 않는다
- **부정문 회피.** "걱정하지 마세요"는 걱정을 떠올리게 한다. 긍정 서술로 바꾼다
- **2인칭 현재형.** 과거·미래·가정을 쓰지 않는다
- **새 정보 금지 (`drift` 이후).** 처리할 것이 남으면 잠들지 못한다
- **문장이 뒤로 갈수록 짧아진다.** `settle` 25~40자 → `fade`·`tail` 10자 이하
- **반복은 결함이 아니다.** 같은 말이 돌아오는 것이 안정감을 준다

---

## 5. 상태별 강조

상태(racing/anxious/…)는 **어느 층을 길게 쓰느냐**로 표현한다.
층을 없애지 않는다 — 순서는 항상 같고 분량만 달라진다.

2026-09-03 부터는 여기에 **기법 풀**도 얹는다. 기존 조각을 갈아치우는 게 아니라
같은 층에 다른 기법 조각을 더해 두고, 밤마다 섞여 나오게 한다.

| 상태 | 강조 | 이유 |
|---|---|---|
| 생각이 안 멈춰요 | `breath` + `drift` | 셀 것을 오래 주고, 심상으로 인지를 옮긴다. `drift` 에 **인지 섞기** 풀(무관한 2~4음절 명사 12개, `shuffle: 2`) |
| 마음이 불안해요 | `breath` | 날숨 늘이기가 부교감을 직접 건드린다 |
| 몸은 피곤한데 못 자요 | `release` | 몸은 준비됐는데 안 꺼졌다. 바디스캔이 스위치다. 이완 두 층에 **PMR** 풀(힘 줬다 빼기) |
| 자다가 깼어요 | `settle` + `fade` 만 | 이미 밤중이다. 다시 깨우면 안 된다 (~90초) |
| 그냥 잠이 안 와요 | 균등 | |

`shuffle: N` 은 N번째 토막부터 순서를 무작위로 트는 앱 기능이다(`audio.js _speak`).
도입 문장은 고정되고 단어만 밤마다 다른 순서로 나온다 — 무관한 단어를 하나씩
떠올리는 기법이라 순서 자체에 뜻이 없다. 단어 사이 침묵은 기존 공식이 저절로
~7초를 만든다(연구 프로토콜 8초).

낮 세션은 별개다.
- `calm` (마음 가라앉히기): `settle` + `breath` + 짧은 `fade`. 3분. **깨어 있는 채로 끝난다**
- `nap` (낮잠): `settle` + `release`. 심상 없음 — 깊이 들어가면 일어나기 힘들다

---

## 6. 구현

### 코드는 고치지 않는다

`buildSequence` 는 `state.sequence` 배열을 그대로 따르고 `state[층이름]` 에서 뽑는다.
층 이름이 무엇이든 상관하지 않는다. **층 추가는 JSON 편집만으로 끝난다.**
5층 → 7층 전환도 이 배열만 늘렸다.

```json
"sequence": ["settle", "breath", "release", "release2", "drift", "fade", "tail"]
```

### 침묵만 코드가 필요하다

층마다 침묵 배수를 둔다. `sentenceGap` 설정값에 곱한다.
현행값은 `audio.js` 의 `LAYER_GAP` 이 진실 소스다.

```
침묵 LAYER_GAP  settle 1.2 · breath 1.8 · release 3.6 · release2 3.6
                drift 3.0 · fade 3.2 · tail 4.6
음량 LAYER_VOL  settle 1.00 · breath 0.98 · release 0.94 · release2 0.90
                drift 0.85 · fade 0.78 · tail 0.70   (후렴은 앞 층을 물려받는다)
```

처음 잡은 값(1.0~2.5)은 2026-09-02 에 전부 올렸다 — 그 배수로는 밤 세션이 7분에
끝났다. 상태별로 더 줄여야 하면 매니페스트의 `gapScale` 로 덮는다(`awoken: 0.7`).

사용자가 설정에서 `sentenceGap` 을 움직이면 전체가 비례해서 늘고 준다.

### 조각 수

**현재 120조각(후렴 6 포함) · mp3 1,095개** (2026-09-04, `data/narration.json` 실측).

| 상태 | 층 | 조각 | 조합 |
|---|---|---|---|
| racing | 7 | 24 (+후렴 1) | 4,374 |
| wired | 7 | 27 (+후렴 1) | 8,748 |
| anxious | 7 | 21 (+후렴 1) | 2,187 |
| unknown | 7 | 21 (+후렴 1) | 2,187 |
| awoken | 2 | 6 | 9 |
| calm | 3 | 9 (+후렴 1) | 27 |
| nap | 2 | 6 (+후렴 1) | 9 |

후렴은 뽑기를 거치지 않으므로 조합 수를 늘리지 않는다.

층당 3개가 기본이고, 기법 풀을 얹은 층(racing `drift` 6 · wired `release`·`release2` 6)만
더 많다. 층이 늘어난 만큼 조합이 곱으로 커지므로 층당 3개로도 반복이 들리지 않는다.

---

## 7. 목소리에 대한 함의

이 정의가 정해지면 필요한 목소리도 정해진다.

> 이 절은 Edge TTS 를 쓰던 2026-08-12 판단이다. **엔진은 그 뒤 VoxCPM2(시드 42 고정,
> 남성)로 바뀌었다** — 확정값과 배포 주의사항은 CLAUDE.md 결정표와 STATUS.md
> '음성 엔진 교체' 절이 진실 소스다. 아래 표는 왜 엔진을 바꿨는지의 기록으로 남긴다.

| 필요한 것 | 당시 Edge ko-KR |
|---|---|
| 낮은 각성. 따뜻하되 밝지 않게 | ✗ `VoicePersonalities: Friendly, Positive` |
| 문장 끝이 내려가는 억양 | △ **어미를 서술형으로 바꾸면 개선된다** |
| 숨소리 섞인 낮은 성량 | ✗ 스타일 지원 없음 (ko-KR 은 calm/whispering 부재) |
| 느리되 늘어지지 않게 | ✓ 침묵으로 해결됨 |

**엔진을 바꾸기 전에 어미부터 바꾼다.** 그것이 공짜이고, 바꾼 뒤에도 계속 유효하다 —
4.1 의 어미 규칙은 엔진이 바뀐 지금도 그대로 적용된다.
