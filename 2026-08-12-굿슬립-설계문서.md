# 굿슬립 — 설계문서

> 작성: 2026-08-12
> 상태: 설계 확정, 구현 대기
> 형태: 모바일 웹 (정적 파일, 빌드 없음)

---

## 1. 왜 만드는가

기존 두 앱을 실제로 써본 경험에서 출발한다.

**Calm (Clam)**

| 좋았던 것 | 나빴던 것 |
|---|---|
| 백색소음·자연의 소리가 풍부 | 명상 콘텐츠가 결국 같은 것만 반복됨 |
| 자연 이미지·배경 비주얼 | |

**Renight (나이틀리, Munice)**

| 좋았던 것 | 나빴던 것 |
|---|---|
| 몇 시간 잘지 정하면 알람 → 실제 수면시간·수면부채 리포트 | 모노럴 비트가 듣기 좋은 소리가 아님 |
| 상태를 골라서 그에 맞는 나레이션 | "상태 맞춤"이라지만 결국 비슷한 것만 반복됨 |

두 앱의 불만이 **하나로 수렴한다: 반복.**
Renight는 AI가 매일 새로 생성한다고 하는데도 사용자는 "똑같다"고 느꼈다. 즉 생성 여부가 아니라 **체감 다양성**이 문제다.

**굿슬립의 정의**

> 매일 밤 다른 이야기가 나오는 수면 앱. + 아침에 내가 몇 시간 잤고 얼마나 빚졌는지 알려주는 앱.

### 이름

`굿슬립`. 담백한 이름이므로 제품 전체 말투도 담백하게 간다 — 시적인 문장 대신 짧은 평서문.

**이름은 나중에 바꿀 수 있게 설계한다.** 한글 "굿슬립"은 앱스토어에 겹치는 것이 없지만, 영문 `GoodSleep`은 동종 수면앱이 이미 여러 개 존재한다. 또한 수면앱에 대해 지나치게 서술적이어서 상표로 등록하기 어렵고 등록되어도 타인을 막기 어렵다 — 소유할 수 없는 이름이다.

사업화 시점의 문제이므로 지금 결정하지 않는다. 대신 **표시 이름을 상수 한 곳(`app.js`의 `APP_NAME`)에서만 참조**하고, 폴더·코드·저장소 키는 `goodsleep`으로 고정한다. 나중에 이름을 바꿔도 한 줄 수정으로 끝난다.

---

## 2. 제품 구성 — 밤 90% / 낮 10%

이 앱은 **불면 앱**이다. 명상 앱이 아니다.

- **밤 (90%)** — 홈 전체. 상태 선택 → 나레이션 + 사운드 → 수면 → 알람 → 리포트
- **낮 (10%)** — 홈 구석의 작은 진입점 하나. 두 가지가 들어간다.
  - 3~10분짜리 마음안정 명상 / 호흡 세션
  - **낮잠** (§6.3)

  둘 다 나레이션 조립 엔진과 사운드 믹서를 그대로 재사용하므로 추가 구조가 거의 없다.

**낮 세션은 밤 기록을 건드리지 않는다.** 명상·호흡은 아무 기록도 남기지 않고, 낮잠은 밤 기록과 완전히 분리된 저장소에 쌓인다. "어젯밤 몇 시간 잤나"에 낮잠이 섞이면 그 숫자는 무의미해진다.

---

## 3. 화면 흐름

```
[홈]
  오늘 밤 상태 5개 중 선택
  (구석에 "낮에 잠깐" 진입점 → 명상·호흡 / 낮잠)
       ↓
[준비]
  몇 시간 잘지 · 사운드 믹서 · 씬 선택
       ↓
[재생]
  배경 씬 + 호흡 가이드 + 나레이션 조립 재생
  나레이션 끝나면 페이드아웃 → 사운드만 밤새 유지
       ↓
[알람]
  설정 시각, 무음에서 60초 페이드인
       ↓
[리포트]
  잔 시간 · 목표 대비 · 누적 수면부채 · 14일 추이
```

### 상태 5종

| 코드 | 라벨 |
|---|---|
| `racing` | 생각이 안 멈춰요 |
| `anxious` | 마음이 불안해요 |
| `wired` | 몸은 피곤한데 못 자요 |
| `awoken` | 자다가 깼어요 |
| `unknown` | 그냥 잠이 안 와요 |

`awoken`은 특수하다 — 이미 밤중이므로 알람 재설정 없이 짧은 나레이션만 재생하고 기존 알람을 유지한다.

---

## 4. 나레이션 조립 엔진 — 이 앱의 핵심

### 왜 미리 만드는가

리서치 결론: **일반 TTS 모델은 긴 침묵을 오류로 간주해 자동으로 줄인다.** 대화용으로 최적화돼 있기 때문이다. 그런데 수면 나레이션은 문장 사이 3~8초 침묵이 본질이다.

따라서 실시간 생성은 구조적으로 부적합하다:

| | 실시간 생성 | 미리 생성 |
|---|---|---|
| 침묵 제어 | 모델이 임의로 축소 | 문장별 렌더 후 무음 직접 삽입 |
| 발음 사고 | 그대로 사용자에게 노출 | 검수 후 폐기 가능 |
| 첫 재생 지연 | 5~30초 | 0 |
| 볼륨 균일성 | 들쭉날쭉 | 일괄 노멀라이즈 |
| 비용 | 매 세션 | 1회 |

### 통짜 트랙이 아니라 조각 3층

통짜 50개는 상태당 10개다. 매일 쓰면 10일이면 반복을 체감한다 — Renight가 실패한 지점이다.

```
상태별:  intro(도입) × body(본문) × outro(마무리)
예:      6 × 10 × 5 = 조각 21개 → 조합 300개
```

**이음매가 들리지 않는 이유:** 수면 나레이션은 원래 문장 사이가 침묵이다. 조각 경계가 그 침묵 안에 숨는다. 같은 목소리·같은 설정·같은 룸톤으로 렌더하면 구분되지 않는다.

### 중복 회피

층마다 최근 뽑힌 조각 id 큐를 `localStorage`에 유지하고, 다음 선택 시 후보에서 제외한다.

```
history["racing.body"] = ["b7", "b2", "b9", "b1"]   // 최근 4개
```

배제 폭은 `clamp(round(pool.length / 3), 1, 5)`.

**처음엔 `pool.length - 1`개를 배제하려 했고, 그건 틀렸다.** 후보가 항상 하나만 남아 선택이 결정론적 순환이 되기 때문이다 — 구현 후 테스트에서 조합 36개짜리 상태가 12개만 돌았다. 반복을 막으려던 규칙이 반복을 만들었다. 1/3만 배제하면 직전 조각은 여전히 확실히 걸러지면서(연속 중복 없음) 후보가 여럿 남아 실제로 무작위가 된다.

**풀이 작아도 데드락에 빠지지 않는다** — 후보가 전부 배제되면 큐에서 가장 오래된 것부터 되살린다.

### 파일 없이도 오늘 돌아간다

조각은 두 형태를 모두 허용한다.

```json
{ "id": "b3", "file": "audio/racing/b3.mp3" }
{ "id": "b3", "text": "숨을 천천히 내쉬어 보세요. 서두르지 않아도 됩니다." }
```

- `file` 있으면 → 오디오 파일 재생
- `file` 없고 `text`만 있으면 → 브라우저 내장 `speechSynthesis`로 임시 재생

**결과: 오디오 0개인 오늘도 전체 흐름을 폰에서 테스트할 수 있다.** 나중에 TTS로 뽑은 mp3를 폴더에 넣고 매니페스트에 `file` 한 줄 추가하면 그대로 승격된다. 코드 변경 없음.

### 재생 시퀀스

```
intro → 침묵(6초) → body → 침묵(6초) → outro → 20초 페이드아웃 → 사운드만 잔류
```

침묵 길이는 설정에서 조절 가능(4~12초).

---

## 5. 사운드 믹서

Calm의 강점을 그대로. 여러 소리를 개별 볼륨으로 겹쳐 듣는다.

- 레이어 각각 독립 볼륨 슬라이더, 무한 루프
- 나레이션은 별도 레이어 — 나레이션 재생 중 배경 사운드는 자동으로 60%까지 덕킹
- 조합을 프리셋으로 저장
- 매니페스트(`data/sounds.json`) 기반이라 음원 추가 시 JSON만 수정

필요 음원 목록:

```
비 · 파도 · 바람 · 모닥불 · 시냇물 · 밤 풀벌레 · 선풍기
백색 노이즈 · 핑크 노이즈 · 브라운 노이즈
```

---

## 6. 수면 타이머 · 알람 · 수면부채

### 6.1 기상 시각 = 알람을 끄는 순간

웹앱이 확실히 알 수 있는 유일한 신호다. **스누즈는 무시하고 최종 해제만 기록한다.**

### 6.2 낮잠 기록

낮잠도 수면이다. 기록하지 않으면 부채가 실제보다 크게 나온다.

두 경로 모두 제공한다.

**(1) 낮잠 타이머 — 지금 잘 때**
```
20분 / 30분 / 60분 / 직접 입력  →  사운드 + 짧은 나레이션  →  알람
일어나서 알람을 끄면 실제 낮잠 시간이 기록된다 (밤과 동일한 원리)
```
짧은 나레이션은 `outro`를 뺀 `intro + body` 한 벌만 쓴다. 낮잠은 깊이 들어가면 안 되기 때문이다.

**(2) 수동 입력 — 이미 자고 난 뒤**
```
리포트 화면에서 날짜와 분(minute)을 직접 추가
```

**밤 기록과 분리해서 저장한다.** 이유가 셋 있다.

1. 하루에 낮잠을 두 번 이상 잘 수 있다
2. 밤에 앱을 안 쓴 날에도 낮잠은 기록할 수 있어야 한다 — 밤 기록에 붙여두면 넣을 곳이 없어진다
3. "어젯밤 잔 시간"이라는 숫자가 오염되지 않는다

낮잠 길이에 상한을 두지 않는다. 3시간을 잤다면 그건 사용자의 실제 수면이고, 그대로 부채에서 차감되는 게 맞다.

### 6.3 계산식

```
필요수면    = 8h            (설정에서 조절)
잠드는시간  = 15분          (설정에서 조절 — 캘리브레이션 노브)
실제수면    = (알람해제 − 취침기록) − 잠드는시간
부채        = clamp( Σ(최근 14일 밤) (필요 − 실제) − Σ(최근 14일 낮잠), 0, 20h )
```

낮잠은 그날 밤의 필요수면을 깎는 게 아니라 **총 부채에서 직접 차감**한다. 식이 단순해지고, 낮잠이 밤 기록 없는 날에 있어도 그대로 반영된다.

설계 판단 세 가지:

1. **잠드는 시각은 감지할 수 없다.** 웹에서는 불가능하다. 가정값을 두고 사용자가 직접 튜닝하게 한다. 실제 하드웨어·인체는 항상 모델과 어긋나므로 조절 손잡이를 남긴다.
2. **밤별로 0에서 자르지 않는다.** 많이 잔 날의 잉여가 부채를 갚는다 — 실제 단기 수면부채의 작동 방식에 더 가깝고, 식도 더 단순하다.
3. **상한 20h.** 부채가 무한히 쌓이면 숫자가 절망스러워지고 사용자는 앱을 지운다.

**기록이 없는 밤은 계산에 포함하지 않는다.** 앱을 안 쓴 날을 8시간 부채로 잡으면 안 된다.

### 6.4 리포트

- 어젯밤 잔 시간 / 목표 대비 차이
- 누적 수면부채
- 최근 14일 막대 추이 — 밤 수면과 낮잠을 **다른 색으로 쌓아** 표시한다
- 낮잠 수동 추가 진입점

---

## 7. 비주얼 씬

캔버스로 생성한다. 이미지 파일 0개.

```
별하늘 · 창밖 비 · 파도 · 모닥불
```

- 사운드 레이어와 짝이 맞음 (비 소리 ↔ 창밖 비)
- 시간이 지나면 자동으로 어두워짐
- 호흡 가이드는 씬 위에 겹치는 원 — 들숨에 확장, 날숨에 수축

**나중에 실제 영상·이미지로 교체 가능**하도록 씬 인터페이스를 분리해 둔다.

---

## 8. 기술 구조

### 결정: 실시간 합성이 아니라 오디오 파일

모바일에서 화면을 끄면 iOS는 Web Audio 컨텍스트를 정지시킨다. 수면 앱에서 화면을 켜둘 수는 없다.

- 실시간 합성 → 화면 잠김 → **끊김**
- 오디오 파일 재생 → 음악 앱처럼 백그라운드 유지 + 잠금화면 컨트롤

파일 방식이 기술적으로 유일한 길이다.

### 백그라운드 유지 전략

1. 밤새 **무음 루프 트랙**을 재생해 탭을 살려둔다
2. **MediaSession API**로 잠금화면에 앱 이름·씬을 노출하고 재생/정지를 연결한다
3. 알람 시각은 `setTimeout`에 의존하지 않고 **`Date.now()` 벽시계로 재계산**한다 — `visibilitychange` 시점과 30초 간격으로 검사. 브라우저 타이머 스로틀링과 무관해진다

### 알람 신뢰성 — 정직한 한계

이건 웹앱이다. 네이티브 알람만큼 100%는 아니다. 브라우저가 탭을 완전히 종료하면 알람은 울리지 않는다. **첫 며칠은 폰 기본 알람을 백업으로 함께 켜둘 것을 앱이 직접 안내한다.**

### PWA

`manifest.json`만 포함한다 (service worker 제외). 홈 화면에 추가하면 주소창이 사라져 밤에 훨씬 낫고, 백그라운드 오디오도 더 안정적이다. 오프라인 캐시는 v1 범위 밖.

### 파일 구조

```
굿슬립/
  index.html
  style.css
  manifest.json
  app.js            화면 전환 · 전역 상태
  audio.js          믹서 · 백그라운드 유지 · MediaSession
  narration.js      조립 엔진 · 중복 회피
  sleep.js          타이머 · 알람 · 수면부채 계산
  scenes.js         캔버스 비주얼
  data/
    narration.json  상태별 조각 매니페스트
    sounds.json     사운드 레이어 매니페스트
  audio/            (오디오 파일을 채워 넣을 폴더)
  test.js           부채 계산 + 중복 회피 검증
```

빌드 없음. 순수 정적 파일 + ES 모듈. 로컬 서버로 띄워 폰에서 바로 테스트하고, 나중에 GitHub Pages에 그대로 올릴 수 있다.

---

## 9. 데이터 스키마

### `data/narration.json`

```json
{
  "racing": {
    "label": "생각이 안 멈춰요",
    "intro": [
      { "id": "i1", "file": "audio/racing/i1.mp3" },
      { "id": "i2", "text": "눈을 감고, 지금 이 자리를 느껴보세요." }
    ],
    "body":  [ { "id": "b1", "text": "..." } ],
    "outro": [ { "id": "o1", "text": "..." } ]
  }
}
```

### `data/sounds.json`

```json
{
  "layers": [
    { "id": "rain",  "label": "비",     "file": "audio/loops/rain.mp3",  "scene": "rain" },
    { "id": "waves", "label": "파도",   "file": "audio/loops/waves.mp3", "scene": "waves" }
  ]
}
```

### `localStorage`

```js
"goodsleep.settings" = {
  needHours: 8,
  sleepOnsetMin: 15,
  gapSeconds: 6,
  narrationVolume: 0.8
}

"goodsleep.nights" = [
  { date: "2026-08-12", bedAt: 1760000000000, wakeAt: 1760028000000, state: "racing" }
]

// 밤과 분리 — 하루에 여러 개, 밤 기록이 없는 날에도 가능
"goodsleep.naps" = [
  { date: "2026-08-12", minutes: 25, at: 1760050000000, source: "timer" },  // "timer" | "manual"
  { date: "2026-08-12", minutes: 40, at: 1760070000000, source: "manual" }
]

"goodsleep.history" = { "racing.body": ["b7", "b2", "b9"] }

"goodsleep.presets" = [ { name: "비 오는 밤", layers: { rain: 0.7, wind: 0.3 } } ]
```

---

## 10. 검증 (`test.js`)

`node test.js` — 프레임워크 없이 `assert`만 사용한다.

**수면부채**
- 14일 전부 정확히 8시간 → 부채 0
- 6시간 × 5일 → 부채 10h
- 부채가 20h를 넘으면 20h로 절삭
- 잉여 수면이 부채를 차감
- 기록 없는 날은 부채에 잡히지 않음
- `wakeAt < bedAt`(잘못된 기록)은 무시

**낮잠**
- 낮잠이 부채를 차감
- 하루에 낮잠 2건이면 합산되어 차감
- **밤 기록이 없는 날의 낮잠도 차감에 반영됨** (분리 저장의 핵심 이유)
- 14일보다 오래된 낮잠은 차감되지 않음
- 낮잠만 있고 밤 기록이 하나도 없으면 부채는 0 (음수로 내려가지 않음)

**중복 회피**
- 연속 두 번 같은 조각이 나오지 않음
- 풀 크기가 3이어도 데드락 없이 무한히 선택 가능
- 풀 크기가 1이면 항상 그것을 반환 (예외 없음)

---

## 11. 오디오 조달 계획

앱과 별개로 병행한다. 매니페스트 방식이므로 언제 채워도 코드 변경이 없다.

**사운드 (무료 음원)**
- Freesound (CC0), Pixabay Audio, YouTube Audio Library
- 루프 이음매가 들리지 않도록 크로스페이드 처리 후 저장

**나레이션 (TTS 배치)**
1. 상태별 조각 대본 작성 (intro/body/outro)
2. 문장 단위로 렌더 (ElevenLabs v3 등 — 한국어 명상 톤 지원 확인됨)
3. 문장 사이 무음 삽입 (모델에 맡기지 않고 후처리로 직접)
4. 볼륨 일괄 노멀라이즈 → mp3
5. 검수 후 `audio/` 에 배치, 매니페스트에 `file` 추가

**우선순위:** `racing`(생각이 안 멈춰요) 한 상태를 먼저 완성해 품질을 확정하고, 그 파이프라인을 나머지 4개에 복제한다.

---

## 12. v1 범위 밖

- 계정 · 서버 · 기기 간 동기화
- 마이크·가속도계를 이용한 실제 수면 단계 측정
- 결제 · 구독
- 오프라인 캐시 (service worker)
- 모노럴/바이노럴 비트 — Renight에서 듣기 싫었던 부분이므로 의도적으로 제외
